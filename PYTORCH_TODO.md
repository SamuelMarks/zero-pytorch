Extracting target APIs from /Users/samuel/repos/zero-pytorch/src...
Scoring compliance...

--- Compliance Report ---
Overall Compliance: 0.0%

Breakdown by Module:
  - torch.nn: 0.0% (0/162)
  - torch.nn.init: 0.0% (0/26)
  - torch.optim: 0.0% (0/14)
  - torch.utils.data: 0.0% (0/23)

Missing APIs (225):

|   | Framework | Namespace | Symbol | FQN | Signature | Docstring |
|---|---|---|---|---|---|---|
| [ ] | torch | torch.nn | AdaptiveAvgPool1d | torch.nn.AdaptiveAvgPool1d | `(output_size: _size_1_t) -> NoneType` | Applies a 1D adaptive average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AdaptiveAvgPool2d | torch.nn.AdaptiveAvgPool2d | `(output_size: _size_2_opt_t) -> NoneType` | Applies a 2D adaptive average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AdaptiveAvgPool3d | torch.nn.AdaptiveAvgPool3d | `(output_size: _size_3_opt_t) -> NoneType` | Applies a 3D adaptive average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AdaptiveLogSoftmaxWithLoss | torch.nn.AdaptiveLogSoftmaxWithLoss | `(in_features: int, n_classes: int, cutoffs: list[int], div_value: float=4.0, head_bias: bool=False, head: Linear, tail: ModuleList, device='```(None)```', dtype='```(None)```') -> NoneType` | Efficient softmax approximation. |
| [ ] | torch | torch.nn | AdaptiveMaxPool1d | torch.nn.AdaptiveMaxPool1d | `(output_size: _size_1_t, return_indices: bool=False) -> NoneType` | Applies a 1D adaptive max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AdaptiveMaxPool2d | torch.nn.AdaptiveMaxPool2d | `(output_size: _size_2_opt_t, return_indices: bool=False) -> NoneType` | Applies a 2D adaptive max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AdaptiveMaxPool3d | torch.nn.AdaptiveMaxPool3d | `(output_size: _size_3_opt_t, return_indices: bool=False) -> NoneType` | Applies a 3D adaptive max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AlphaDropout | torch.nn.AlphaDropout | `(p: float=0.5, inplace: bool | None='```(None)```') -> NoneType` | Applies Alpha Dropout over the input. |
| [ ] | torch | torch.nn | AvgPool1d | torch.nn.AvgPool1d | `(kernel_size: _size_1_t, stride: _size_1_t=':attr:`kernel_size', padding: _size_1_t=0, ceil_mode: bool=False, count_include_pad: bool=True) -> NoneType` | Applies a 1D average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AvgPool2d | torch.nn.AvgPool2d | `(kernel_size: _size_2_t, stride: _size_2_t=':attr:`kernel_size', padding: _size_2_t=0, ceil_mode: bool=False, count_include_pad: bool=True, divisor_override: int | None='```(None)```', __constants__: list=['kernel_size', 'stride', 'padding', 'ceil_mode', 'count_include_pad', 'divisor_override']) -> NoneType` | Applies a 2D average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | AvgPool3d | torch.nn.AvgPool3d | `(kernel_size: _size_3_t, stride: _size_3_t=':attr:`kernel_size', padding: _size_3_t=0, ceil_mode: bool=False, count_include_pad: bool=True, divisor_override: int | None='```(None)```', __constants__: list=['kernel_size', 'stride', 'padding', 'ceil_mode', 'count_include_pad', 'divisor_override']) -> NoneType` | Applies a 3D average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | BCELoss | torch.nn.BCELoss | `(weight: Tensor | None='```(None)```', size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['reduction']) -> NoneType` | Creates a criterion that measures the Binary Cross Entropy between the target and the input proba... |
| [ ] | torch | torch.nn | BCEWithLogitsLoss | torch.nn.BCEWithLogitsLoss | `(weight: Tensor | None='```(None)```', size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', pos_weight: Tensor | None='```(None)```') -> NoneType` | This loss combines a `Sigmoid` layer and the `BCELoss` in one single class. This version is more ... |
| [ ] | torch | torch.nn | BatchNorm1d | torch.nn.BatchNorm1d | `(num_features: int, eps: float=1e-05, momentum: float=0.1, affine: bool=True, track_running_stats: bool=True, device=None, dtype=None) -> NoneType` | Applies Batch Normalization over a 2D or 3D input. |
| [ ] | torch | torch.nn | BatchNorm2d | torch.nn.BatchNorm2d | `(num_features: int, eps: float=1e-05, momentum: float=0.1, affine: bool=True, track_running_stats: bool=True, device=None, dtype=None) -> NoneType` | Applies Batch Normalization over a 4D input. |
| [ ] | torch | torch.nn | BatchNorm3d | torch.nn.BatchNorm3d | `(num_features: int, eps: float=1e-05, momentum: float=0.1, affine: bool=True, track_running_stats: bool=True, device=None, dtype=None) -> NoneType` | Applies Batch Normalization over a 5D input. |
| [ ] | torch | torch.nn | Bilinear | torch.nn.Bilinear | `(in1_features: int, in2_features: int, out_features: int, bias: bool=True, __constants__: list=['in1_features', 'in2_features', 'out_features'], weight: Tensor, device='```(None)```', dtype='```(None)```') -> NoneType` | Applies a bilinear transformation to the incoming data: :math:`y = x_1^T A x_2 + b`. |
| [ ] | torch | torch.nn | CELU | torch.nn.CELU | `(alpha: float=1.0, inplace: bool=False, __constants__: list=['alpha', 'inplace']) -> NoneType` | Applies the CELU function element-wise. |
| [ ] | torch | torch.nn | CTCLoss | torch.nn.CTCLoss | `(blank: int=0, reduction: str | None='mean', __constants__: list=['blank', 'reduction'], zero_infinity: bool=False)` | The Connectionist Temporal Classification loss. |
| [ ] | torch | torch.nn | ChannelShuffle | torch.nn.ChannelShuffle | `(groups: int, __constants__: list=['groups']) -> NoneType` | Divides and rearranges the channels in a tensor. |
| [ ] | torch | torch.nn | CircularPad1d | torch.nn.CircularPad1d | `(padding: tuple[int, int]) -> NoneType` | Pads the input tensor using circular padding of the input boundary. |
| [ ] | torch | torch.nn | CircularPad2d | torch.nn.CircularPad2d | `(padding: tuple[int, int, int, int]) -> NoneType` | Pads the input tensor using circular padding of the input boundary. |
| [ ] | torch | torch.nn | CircularPad3d | torch.nn.CircularPad3d | `(padding: tuple[int, int, int, int, int, int]) -> NoneType` | Pads the input tensor using circular padding of the input boundary. |
| [ ] | torch | torch.nn | ConstantPad1d | torch.nn.ConstantPad1d | `(padding: tuple[int, int], value: float)` | Pads the input tensor boundaries with a constant value. |
| [ ] | torch | torch.nn | ConstantPad2d | torch.nn.ConstantPad2d | `(padding: tuple[int, int, int, int], __constants__: list=['padding', 'value'], value: float) -> NoneType` | Pads the input tensor boundaries with a constant value. |
| [ ] | torch | torch.nn | ConstantPad3d | torch.nn.ConstantPad3d | `(padding: tuple[int, int, int, int, int, int], value: float) -> NoneType` | Pads the input tensor boundaries with a constant value. |
| [ ] | torch | torch.nn | Container | torch.nn.Container | `(kwargs: Any={}) -> NoneType` | Base class for all neural network modules. |
| [ ] | torch | torch.nn | Conv1d | torch.nn.Conv1d | `(__doc__='```(\'Applies a 1D convolution over an input signal composed of several input\\n    planes.\\n\\n    In the simplest case, the output value of the layer with input size\\n    :math:`(N, C_{\\\\text{in}}, L)` and output :math:`(N, C_{\\\\text{out}}, L_{\\\\text{out}})` can be\\n    precisely described as:\\n\\n    .. math::\\n        \\\\text{out}(N_i, C_{\\\\text{out}_j}) = \\\\text{bias}(C_{\\\\text{out}_j}) +\\n        \\\\sum_{k = 0}^{C_{in} - 1} \\\\text{weight}(C_{\\\\text{out}_j}, k)\\n        \\\\star \\\\text{input}(N_i, k)\\n\\n    where :math:`\\\\star` is the valid `cross-correlation`_ operator,\\n    :math:`N` is a batch size, :math:`C` denotes a number of channels,\\n    :math:`L` is a length of signal sequence.\\n    \' + "\\n\\n    This module supports :ref:`TensorFloat32<tf32_on_ampere>`.\\n\\n    On certain ROCm devices, when using float16 inputs this module will use :ref:`different precision<fp16_on_mi200>` for backward.\\n\\n    * :attr:`stride` controls the stride for the cross-correlation, a single\\n      number or a one-element tuple.\\n\\n    * :attr:`padding` controls the amount of padding applied to the input. It\\n      can be either a string {{\'valid\', \'same\'}} or a tuple of ints giving the\\n      amount of implicit padding applied on both sides.\\n\\n    * :attr:`dilation` controls the spacing between the kernel points; also\\n      known as the à trous algorithm. It is harder to describe, but this `link`_\\n      has a nice visualization of what :attr:`dilation` does.\\n\\n    {groups_note}\\n\\n    Note:\\n        {depthwise_separable_note}\\n    Note:\\n        {cudnn_reproducibility_note}\\n\\n    Note:\\n        ``padding=\'valid\'`` is the same as no padding. ``padding=\'same\'`` pads\\n        the input so the output has the shape as the input. However, this mode\\n        doesn\'t support any stride values other than 1.\\n\\n    Note:\\n        This module supports complex data types i.e. ``complex32, complex64, complex128``.\\n\\n    Args:\\n        in_channels (int): Number of channels in the input image\\n        out_channels (int): Number of channels produced by the convolution\\n        kernel_size (int or tuple): Size of the convolving kernel\\n        stride (int or tuple, optional): Stride of the convolution. Default: 1\\n        padding (int, tuple or str, optional): Padding added to both sides of\\n            the input. Default: 0\\n        dilation (int or tuple, optional): Spacing between kernel\\n            elements. Default: 1\\n        groups (int, optional): Number of blocked connections from input\\n            channels to output channels. Default: 1\\n        bias (bool, optional): If ``True``, adds a learnable bias to the\\n            output. Default: ``True``\\n        padding_mode (str, optional): ``\'zeros\'``, ``\'reflect\'``,\\n            ``\'replicate\'`` or ``\'circular\'``. Default: ``\'zeros\'``\\n\\n    ".format(**reproducibility_notes, **convolution_notes) + \'\\n\\n    Shape:\\n        - Input: :math:`(N, C_{in}, L_{in})` or :math:`(C_{in}, L_{in})`\\n        - Output: :math:`(N, C_{out}, L_{out})` or :math:`(C_{out}, L_{out})`, where\\n\\n          .. math::\\n              L_{out} = \\\\left\\\\lfloor\\\\frac{L_{in} + 2 \\\\times \\\\text{padding} - \\\\text{dilation}\\n                        \\\\times (\\\\text{kernel\\\\_size} - 1) - 1}{\\\\text{stride}} + 1\\\\right\\\\rfloor\\n\\n    Attributes:\\n        weight (Tensor): the learnable weights of the module of shape\\n            :math:`(\\\\text{out\\\\_channels},\\n            \\\\frac{\\\\text{in\\\\_channels}}{\\\\text{groups}}, \\\\text{kernel\\\\_size})`.\\n            The values of these weights are sampled from\\n            :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n            :math:`k = \\\\frac{groups}{C_\\\\text{in} * \\\\text{kernel\\\\_size}}`\\n        bias (Tensor):   the learnable bias of the module of shape\\n            (out_channels). If :attr:`bias` is ``True``, then the values of these weights are\\n            sampled from :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n            :math:`k = \\\\frac{groups}{C_\\\\text{in} * \\\\text{kernel\\\\_size}}`\\n\\n    Examples::\\n\\n        >>> m = nn.Conv1d(16, 33, 3, stride=2)\\n        >>> input = torch.randn(20, 16, 50)\\n        >>> output = m(input)\\n\\n    .. _cross-correlation:\\n        https://en.wikipedia.org/wiki/Cross-correlation\\n\\n    .. _link:\\n        https://github.com/vdumoulin/conv_arithmetic/blob/master/README.md\\n    \')```', device='```(None)```', padding: str | _size_1_t=0, out_channels: int, groups: int=1, dilation: _size_1_t=1, in_channels: int, bias: bool=True, padding_mode: str='zeros', dtype='```(None)```', kernel_size: _size_1_t, stride: _size_1_t=1, transposed: bool, output_padding: tuple) -> NoneType` | Applies a 1D convolution over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | Conv2d | torch.nn.Conv2d | `(__doc__='```(\'Applies a 2D convolution over an input signal composed of several input\\n    planes.\\n\\n    In the simplest case, the output value of the layer with input size\\n    :math:`(N, C_{\\\\text{in}}, H, W)` and output :math:`(N, C_{\\\\text{out}}, H_{\\\\text{out}}, W_{\\\\text{out}})`\\n    can be precisely described as:\\n\\n    .. math::\\n        \\\\text{out}(N_i, C_{\\\\text{out}_j}) = \\\\text{bias}(C_{\\\\text{out}_j}) +\\n        \\\\sum_{k = 0}^{C_{\\\\text{in}} - 1} \\\\text{weight}(C_{\\\\text{out}_j}, k) \\\\star \\\\text{input}(N_i, k)\\n\\n\\n    where :math:`\\\\star` is the valid 2D `cross-correlation`_ operator,\\n    :math:`N` is a batch size, :math:`C` denotes a number of channels,\\n    :math:`H` is a height of input planes in pixels, and :math:`W` is\\n    width in pixels.\\n    \' + "\\n\\n    This module supports :ref:`TensorFloat32<tf32_on_ampere>`.\\n\\n    On certain ROCm devices, when using float16 inputs this module will use :ref:`different precision<fp16_on_mi200>` for backward.\\n\\n    * :attr:`stride` controls the stride for the cross-correlation, a single\\n      number or a tuple.\\n\\n    * :attr:`padding` controls the amount of padding applied to the input. It\\n      can be either a string {{\'valid\', \'same\'}} or an int / a tuple of ints giving the\\n      amount of implicit padding applied on both sides.\\n\\n    * :attr:`dilation` controls the spacing between the kernel points; also\\n      known as the à trous algorithm. It is harder to describe, but this `link`_\\n      has a nice visualization of what :attr:`dilation` does.\\n\\n\\n    {groups_note}\\n\\n    The parameters :attr:`kernel_size`, :attr:`stride`, :attr:`padding`, :attr:`dilation` can either be:\\n\\n        - a single ``int`` -- in which case the same value is used for the height and width dimension\\n        - a ``tuple`` of two ints -- in which case, the first `int` is used for the height dimension,\\n          and the second `int` for the width dimension\\n\\n    Note:\\n        {depthwise_separable_note}\\n\\n    Note:\\n        {cudnn_reproducibility_note}\\n\\n    Note:\\n        ``padding=\'valid\'`` is the same as no padding. ``padding=\'same\'`` pads\\n        the input so the output has the shape as the input. However, this mode\\n        doesn\'t support any stride values other than 1.\\n\\n    Note:\\n        This module supports complex data types i.e. ``complex32, complex64, complex128``.\\n\\n    Args:\\n        in_channels (int): Number of channels in the input image\\n        out_channels (int): Number of channels produced by the convolution\\n        kernel_size (int or tuple): Size of the convolving kernel\\n        stride (int or tuple, optional): Stride of the convolution. Default: 1\\n        padding (int, tuple or str, optional): Padding added to all four sides of\\n            the input. Default: 0\\n        dilation (int or tuple, optional): Spacing between kernel elements. Default: 1\\n        groups (int, optional): Number of blocked connections from input\\n            channels to output channels. Default: 1\\n        bias (bool, optional): If ``True``, adds a learnable bias to the\\n            output. Default: ``True``\\n        padding_mode (str, optional): ``\'zeros\'``, ``\'reflect\'``,\\n            ``\'replicate\'`` or ``\'circular\'``. Default: ``\'zeros\'``\\n    ".format(**reproducibility_notes, **convolution_notes) + \'\\n\\n    Shape:\\n        - Input: :math:`(N, C_{in}, H_{in}, W_{in})` or :math:`(C_{in}, H_{in}, W_{in})`\\n        - Output: :math:`(N, C_{out}, H_{out}, W_{out})` or :math:`(C_{out}, H_{out}, W_{out})`, where\\n\\n          .. math::\\n              H_{out} = \\\\left\\\\lfloor\\\\frac{H_{in}  + 2 \\\\times \\\\text{padding}[0] - \\\\text{dilation}[0]\\n                        \\\\times (\\\\text{kernel\\\\_size}[0] - 1) - 1}{\\\\text{stride}[0]} + 1\\\\right\\\\rfloor\\n\\n          .. math::\\n              W_{out} = \\\\left\\\\lfloor\\\\frac{W_{in}  + 2 \\\\times \\\\text{padding}[1] - \\\\text{dilation}[1]\\n                        \\\\times (\\\\text{kernel\\\\_size}[1] - 1) - 1}{\\\\text{stride}[1]} + 1\\\\right\\\\rfloor\\n\\n    Attributes:\\n        weight (Tensor): the learnable weights of the module of shape\\n            :math:`(\\\\text{out\\\\_channels}, \\\\frac{\\\\text{in\\\\_channels}}{\\\\text{groups}},`\\n            :math:`\\\\text{kernel\\\\_size[0]}, \\\\text{kernel\\\\_size[1]})`.\\n            The values of these weights are sampled from\\n            :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n            :math:`k = \\\\frac{groups}{C_\\\\text{in} * \\\\prod_{i=0}^{1}\\\\text{kernel\\\\_size}[i]}`\\n        bias (Tensor):   the learnable bias of the module of shape\\n            (out_channels). If :attr:`bias` is ``True``,\\n            then the values of these weights are\\n            sampled from :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n            :math:`k = \\\\frac{groups}{C_\\\\text{in} * \\\\prod_{i=0}^{1}\\\\text{kernel\\\\_size}[i]}`\\n\\n    Examples:\\n\\n        >>> # With square kernels and equal stride\\n        >>> m = nn.Conv2d(16, 33, 3, stride=2)\\n        >>> # non-square kernels and unequal stride and with padding\\n        >>> m = nn.Conv2d(16, 33, (3, 5), stride=(2, 1), padding=(4, 2))\\n        >>> # non-square kernels and unequal stride and with padding and dilation\\n        >>> m = nn.Conv2d(16, 33, (3, 5), stride=(2, 1), padding=(4, 2), dilation=(3, 1))\\n        >>> input = torch.randn(20, 16, 50, 100)\\n        >>> output = m(input)\\n\\n    .. _cross-correlation:\\n        https://en.wikipedia.org/wiki/Cross-correlation\\n\\n    .. _link:\\n        https://github.com/vdumoulin/conv_arithmetic/blob/master/README.md\\n    \')```', device='```(None)```', padding: str | _size_2_t=0, out_channels: int, groups: int=1, dilation: _size_2_t=1, in_channels: int, bias: bool=True, padding_mode: str='zeros', dtype='```(None)```', kernel_size: _size_2_t, stride: _size_2_t=1, transposed: bool, output_padding: tuple) -> NoneType` | Applies a 2D convolution over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | Conv3d | torch.nn.Conv3d | `(__doc__='```(\'Applies a 3D convolution over an input signal composed of several input\\n    planes.\\n\\n    In the simplest case, the output value of the layer with input size :math:`(N, C_{in}, D, H, W)`\\n    and output :math:`(N, C_{out}, D_{out}, H_{out}, W_{out})` can be precisely described as:\\n\\n    .. math::\\n        out(N_i, C_{out_j}) = bias(C_{out_j}) +\\n                                \\\\sum_{k = 0}^{C_{in} - 1} weight(C_{out_j}, k) \\\\star input(N_i, k)\\n\\n    where :math:`\\\\star` is the valid 3D `cross-correlation`_ operator\\n    \' + "\\n\\n    This module supports :ref:`TensorFloat32<tf32_on_ampere>`.\\n\\n    On certain ROCm devices, when using float16 inputs this module will use :ref:`different precision<fp16_on_mi200>` for backward.\\n\\n    * :attr:`stride` controls the stride for the cross-correlation.\\n\\n    * :attr:`padding` controls the amount of padding applied to the input. It\\n      can be either a string {{\'valid\', \'same\'}} or a tuple of ints giving the\\n      amount of implicit padding applied on both sides.\\n\\n    * :attr:`dilation` controls the spacing between the kernel points; also known as the à trous algorithm.\\n      It is harder to describe, but this `link`_ has a nice visualization of what :attr:`dilation` does.\\n\\n\\n    {groups_note}\\n\\n    The parameters :attr:`kernel_size`, :attr:`stride`, :attr:`padding`, :attr:`dilation` can either be:\\n\\n        - a single ``int`` -- in which case the same value is used for the depth, height and width dimension\\n        - a ``tuple`` of three ints -- in which case, the first `int` is used for the depth dimension,\\n          the second `int` for the height dimension and the third `int` for the width dimension\\n\\n    Note:\\n        {depthwise_separable_note}\\n\\n    Note:\\n        {cudnn_reproducibility_note}\\n\\n    Note:\\n        ``padding=\'valid\'`` is the same as no padding. ``padding=\'same\'`` pads\\n        the input so the output has the shape as the input. However, this mode\\n        doesn\'t support any stride values other than 1.\\n\\n    Note:\\n        This module supports complex data types i.e. ``complex32, complex64, complex128``.\\n\\n    Args:\\n        in_channels (int): Number of channels in the input image\\n        out_channels (int): Number of channels produced by the convolution\\n        kernel_size (int or tuple): Size of the convolving kernel\\n        stride (int or tuple, optional): Stride of the convolution. Default: 1\\n        padding (int, tuple or str, optional): Padding added to all six sides of\\n            the input. Default: 0\\n        dilation (int or tuple, optional): Spacing between kernel elements. Default: 1\\n        groups (int, optional): Number of blocked connections from input channels to output channels. Default: 1\\n        bias (bool, optional): If ``True``, adds a learnable bias to the output. Default: ``True``\\n        padding_mode (str, optional): ``\'zeros\'``, ``\'reflect\'``, ``\'replicate\'`` or ``\'circular\'``. Default: ``\'zeros\'``\\n    ".format(**reproducibility_notes, **convolution_notes) + \'\\n\\n    Shape:\\n        - Input: :math:`(N, C_{in}, D_{in}, H_{in}, W_{in})` or :math:`(C_{in}, D_{in}, H_{in}, W_{in})`\\n        - Output: :math:`(N, C_{out}, D_{out}, H_{out}, W_{out})` or :math:`(C_{out}, D_{out}, H_{out}, W_{out})`,\\n          where\\n\\n          .. math::\\n              D_{out} = \\\\left\\\\lfloor\\\\frac{D_{in} + 2 \\\\times \\\\text{padding}[0] - \\\\text{dilation}[0]\\n                    \\\\times (\\\\text{kernel\\\\_size}[0] - 1) - 1}{\\\\text{stride}[0]} + 1\\\\right\\\\rfloor\\n\\n          .. math::\\n              H_{out} = \\\\left\\\\lfloor\\\\frac{H_{in} + 2 \\\\times \\\\text{padding}[1] - \\\\text{dilation}[1]\\n                    \\\\times (\\\\text{kernel\\\\_size}[1] - 1) - 1}{\\\\text{stride}[1]} + 1\\\\right\\\\rfloor\\n\\n          .. math::\\n              W_{out} = \\\\left\\\\lfloor\\\\frac{W_{in} + 2 \\\\times \\\\text{padding}[2] - \\\\text{dilation}[2]\\n                    \\\\times (\\\\text{kernel\\\\_size}[2] - 1) - 1}{\\\\text{stride}[2]} + 1\\\\right\\\\rfloor\\n\\n    Attributes:\\n        weight (Tensor): the learnable weights of the module of shape\\n                         :math:`(\\\\text{out\\\\_channels}, \\\\frac{\\\\text{in\\\\_channels}}{\\\\text{groups}},`\\n                         :math:`\\\\text{kernel\\\\_size[0]}, \\\\text{kernel\\\\_size[1]}, \\\\text{kernel\\\\_size[2]})`.\\n                         The values of these weights are sampled from\\n                         :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{in} * \\\\prod_{i=0}^{2}\\\\text{kernel\\\\_size}[i]}`\\n        bias (Tensor):   the learnable bias of the module of shape (out_channels). If :attr:`bias` is ``True``,\\n                         then the values of these weights are\\n                         sampled from :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{in} * \\\\prod_{i=0}^{2}\\\\text{kernel\\\\_size}[i]}`\\n\\n    Examples::\\n\\n        >>> # With square kernels and equal stride\\n        >>> m = nn.Conv3d(16, 33, 3, stride=2)\\n        >>> # non-square kernels and unequal stride and with padding\\n        >>> m = nn.Conv3d(16, 33, (3, 5, 2), stride=(2, 1, 1), padding=(4, 2, 0))\\n        >>> input = torch.randn(20, 16, 10, 50, 100)\\n        >>> output = m(input)\\n\\n    .. _cross-correlation:\\n        https://en.wikipedia.org/wiki/Cross-correlation\\n\\n    .. _link:\\n        https://github.com/vdumoulin/conv_arithmetic/blob/master/README.md\\n    \')```', device='```(None)```', padding: str | _size_3_t=0, out_channels: int, groups: int=1, dilation: _size_3_t=1, in_channels: int, bias: bool=True, padding_mode: str='zeros', dtype='```(None)```', kernel_size: _size_3_t, stride: _size_3_t=1, transposed: bool, output_padding: tuple) -> NoneType` | Applies a 3D convolution over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | ConvTranspose1d | torch.nn.ConvTranspose1d | `(__doc__="```('Applies a 1D transposed convolution operator over an input image\\n    composed of several input planes.\\n\\n    This module can be seen as the gradient of Conv1d with respect to its input.\\n    It is also known as a fractionally-strided convolution or\\n    a deconvolution (although it is not an actual deconvolution operation as it does\\n    not compute a true inverse of convolution). For more information, see the visualizations\\n    `here`_ and the `Deconvolutional Networks`_ paper.\\n\\n    This module supports :ref:`TensorFloat32<tf32_on_ampere>`.\\n\\n    On certain ROCm devices, when using float16 inputs this module will use :ref:`different precision<fp16_on_mi200>` for backward.\\n\\n    * :attr:`stride` controls the stride for the cross-correlation.\\n\\n    * :attr:`padding` controls the amount of implicit zero padding on both\\n      sides for ``dilation * (kernel_size - 1) - padding`` number of points. See note\\n      below for details.\\n\\n    * :attr:`output_padding` controls the additional size added to one side\\n      of the output shape. See note below for details.\\n\\n    * :attr:`dilation` controls the spacing between the kernel points; also known as the à trous algorithm.\\n      It is harder to describe, but the link `here`_ has a nice visualization of what :attr:`dilation` does.\\n\\n    {groups_note}\\n\\n    Note:\\n        The :attr:`padding` argument effectively adds ``dilation * (kernel_size - 1) - padding``\\n        amount of zero padding to both sizes of the input. This is set so that\\n        when a :class:`~torch.nn.Conv1d` and a :class:`~torch.nn.ConvTranspose1d`\\n        are initialized with same parameters, they are inverses of each other in\\n        regard to the input and output shapes. However, when ``stride > 1``,\\n        :class:`~torch.nn.Conv1d` maps multiple input shapes to the same output\\n        shape. :attr:`output_padding` is provided to resolve this ambiguity by\\n        effectively increasing the calculated output shape on one side. Note\\n        that :attr:`output_padding` is only used to find output shape, but does\\n        not actually add zero-padding to output.\\n\\n    Note:\\n        In some circumstances when using the CUDA backend with CuDNN, this operator\\n        may select a nondeterministic algorithm to increase performance. If this is\\n        undesirable, you can try to make the operation deterministic (potentially at\\n        a performance cost) by setting ``torch.backends.cudnn.deterministic =\\n        True``.\\n        Please see the notes on :doc:`/notes/randomness` for background.\\n\\n\\n    Args:\\n        in_channels (int): Number of channels in the input image\\n        out_channels (int): Number of channels produced by the convolution\\n        kernel_size (int or tuple): Size of the convolving kernel\\n        stride (int or tuple, optional): Stride of the convolution. Default: 1\\n        padding (int or tuple, optional): ``dilation * (kernel_size - 1) - padding`` zero-padding\\n            will be added to both sides of the input. Default: 0\\n        output_padding (int or tuple, optional): Additional size added to one side\\n            of the output shape. Default: 0\\n        groups (int, optional): Number of blocked connections from input channels to output channels. Default: 1\\n        bias (bool, optional): If ``True``, adds a learnable bias to the output. Default: ``True``\\n        dilation (int or tuple, optional): Spacing between kernel elements. Default: 1\\n    '.format(**reproducibility_notes, **convolution_notes) + '\\n\\n    Shape:\\n        - Input: :math:`(N, C_{in}, L_{in})` or :math:`(C_{in}, L_{in})`\\n        - Output: :math:`(N, C_{out}, L_{out})` or :math:`(C_{out}, L_{out})`, where\\n\\n          .. math::\\n              L_{out} = (L_{in} - 1) \\\\times \\\\text{stride} - 2 \\\\times \\\\text{padding} + \\\\text{dilation}\\n                        \\\\times (\\\\text{kernel\\\\_size} - 1) + \\\\text{output\\\\_padding} + 1\\n\\n    Attributes:\\n        weight (Tensor): the learnable weights of the module of shape\\n                         :math:`(\\\\text{in\\\\_channels}, \\\\frac{\\\\text{out\\\\_channels}}{\\\\text{groups}},`\\n                         :math:`\\\\text{kernel\\\\_size})`.\\n                         The values of these weights are sampled from\\n                         :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{out} * \\\\text{kernel\\\\_size}}`\\n        bias (Tensor):   the learnable bias of the module of shape (out_channels).\\n                         If :attr:`bias` is ``True``, then the values of these weights are\\n                         sampled from :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{out} * \\\\text{kernel\\\\_size}}`\\n\\n    .. _`here`:\\n        https://github.com/vdumoulin/conv_arithmetic/blob/master/README.md\\n\\n    .. _`Deconvolutional Networks`:\\n        https://www.matthewzeiler.com/mattzeiler/deconvolutionalnetworks.pdf\\n    ')```", output_padding: _size_1_t=0, device='```(None)```', padding: _size_1_t=0, out_channels: int, groups: int=1, dilation: _size_1_t=1, in_channels: int, bias: bool=True, padding_mode: str='zeros', dtype='```(None)```', kernel_size: _size_1_t, stride: _size_1_t=1, transposed: _empty) -> NoneType` | Applies a 1D transposed convolution operator over an input image composed of several input planes. |
| [ ] | torch | torch.nn | ConvTranspose2d | torch.nn.ConvTranspose2d | `(__doc__="```('Applies a 2D transposed convolution operator over an input image\\n    composed of several input planes.\\n\\n    This module can be seen as the gradient of Conv2d with respect to its input.\\n    It is also known as a fractionally-strided convolution or\\n    a deconvolution (although it is not an actual deconvolution operation as it does\\n    not compute a true inverse of convolution). For more information, see the visualizations\\n    `here`_ and the `Deconvolutional Networks`_ paper.\\n\\n    This module supports :ref:`TensorFloat32<tf32_on_ampere>`.\\n\\n    On certain ROCm devices, when using float16 inputs this module will use :ref:`different precision<fp16_on_mi200>` for backward.\\n\\n    * :attr:`stride` controls the stride for the cross-correlation. When stride > 1, ConvTranspose2d inserts zeros between input\\n      elements along the spatial dimensions before applying the convolution kernel. This zero-insertion operation is the standard\\n      behavior of transposed convolutions, which can increase the spatial resolution and is equivalent to a learnable\\n      upsampling operation.\\n\\n    * :attr:`padding` controls the amount of implicit zero padding on both\\n      sides for ``dilation * (kernel_size - 1) - padding`` number of points. See note\\n      below for details.\\n\\n    * :attr:`output_padding` controls the additional size added to one side\\n      of the output shape. See note below for details.\\n\\n    * :attr:`dilation` controls the spacing between the kernel points; also known as the à trous algorithm.\\n      It is harder to describe, but the link `here`_ has a nice visualization of what :attr:`dilation` does.\\n\\n    {groups_note}\\n\\n    The parameters :attr:`kernel_size`, :attr:`stride`, :attr:`padding`, :attr:`output_padding`\\n    can either be:\\n\\n        - a single ``int`` -- in which case the same value is used for the height and width dimensions\\n        - a ``tuple`` of two ints -- in which case, the first `int` is used for the height dimension,\\n          and the second `int` for the width dimension\\n\\n    Note:\\n        The :attr:`padding` argument effectively adds ``dilation * (kernel_size - 1) - padding``\\n        amount of zero padding to both sizes of the input. This is set so that\\n        when a :class:`~torch.nn.Conv2d` and a :class:`~torch.nn.ConvTranspose2d`\\n        are initialized with same parameters, they are inverses of each other in\\n        regard to the input and output shapes. However, when ``stride > 1``,\\n        :class:`~torch.nn.Conv2d` maps multiple input shapes to the same output\\n        shape. :attr:`output_padding` is provided to resolve this ambiguity by\\n        effectively increasing the calculated output shape on one side. Note\\n        that :attr:`output_padding` is only used to find output shape, but does\\n        not actually add zero-padding to output.\\n\\n    Note:\\n        {cudnn_reproducibility_note}\\n\\n    Args:\\n        in_channels (int): Number of channels in the input image\\n        out_channels (int): Number of channels produced by the convolution\\n        kernel_size (int or tuple): Size of the convolving kernel\\n        stride (int or tuple, optional): Stride of the convolution. Default: 1\\n        padding (int or tuple, optional): ``dilation * (kernel_size - 1) - padding`` zero-padding\\n            will be added to both sides of each dimension in the input. Default: 0\\n        output_padding (int or tuple, optional): Additional size added to one side\\n            of each dimension in the output shape. Default: 0\\n        groups (int, optional): Number of blocked connections from input channels to output channels. Default: 1\\n        bias (bool, optional): If ``True``, adds a learnable bias to the output. Default: ``True``\\n        dilation (int or tuple, optional): Spacing between kernel elements. Default: 1\\n    '.format(**reproducibility_notes, **convolution_notes) + '\\n\\n    Shape:\\n        - Input: :math:`(N, C_{in}, H_{in}, W_{in})` or :math:`(C_{in}, H_{in}, W_{in})`\\n        - Output: :math:`(N, C_{out}, H_{out}, W_{out})` or :math:`(C_{out}, H_{out}, W_{out})`, where\\n\\n        .. math::\\n              H_{out} = (H_{in} - 1) \\\\times \\\\text{stride}[0] - 2 \\\\times \\\\text{padding}[0] + \\\\text{dilation}[0]\\n                        \\\\times (\\\\text{kernel\\\\_size}[0] - 1) + \\\\text{output\\\\_padding}[0] + 1\\n        .. math::\\n              W_{out} = (W_{in} - 1) \\\\times \\\\text{stride}[1] - 2 \\\\times \\\\text{padding}[1] + \\\\text{dilation}[1]\\n                        \\\\times (\\\\text{kernel\\\\_size}[1] - 1) + \\\\text{output\\\\_padding}[1] + 1\\n\\n    Attributes:\\n        weight (Tensor): the learnable weights of the module of shape\\n                         :math:`(\\\\text{in\\\\_channels}, \\\\frac{\\\\text{out\\\\_channels}}{\\\\text{groups}},`\\n                         :math:`\\\\text{kernel\\\\_size[0]}, \\\\text{kernel\\\\_size[1]})`.\\n                         The values of these weights are sampled from\\n                         :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{out} * \\\\prod_{i=0}^{1}\\\\text{kernel\\\\_size}[i]}`\\n        bias (Tensor):   the learnable bias of the module of shape (out_channels)\\n                         If :attr:`bias` is ``True``, then the values of these weights are\\n                         sampled from :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{out} * \\\\prod_{i=0}^{1}\\\\text{kernel\\\\_size}[i]}`\\n\\n    Examples::\\n\\n        >>> # With square kernels and equal stride\\n        >>> m = nn.ConvTranspose2d(16, 33, 3, stride=2)\\n        >>> # non-square kernels and unequal stride and with padding\\n        >>> m = nn.ConvTranspose2d(16, 33, (3, 5), stride=(2, 1), padding=(4, 2))\\n        >>> input = torch.randn(20, 16, 50, 100)\\n        >>> output = m(input)\\n        >>> # exact output size can be also specified as an argument\\n        >>> input = torch.randn(1, 16, 12, 12)\\n        >>> downsample = nn.Conv2d(16, 16, 3, stride=2, padding=1)\\n        >>> upsample = nn.ConvTranspose2d(16, 16, 3, stride=2, padding=1)\\n        >>> h = downsample(input)\\n        >>> h.size()\\n        torch.Size([1, 16, 6, 6])\\n        >>> output = upsample(h, output_size=input.size())\\n        >>> output.size()\\n        torch.Size([1, 16, 12, 12])\\n\\n    .. _`here`:\\n        https://github.com/vdumoulin/conv_arithmetic/blob/master/README.md\\n\\n    .. _`Deconvolutional Networks`:\\n        https://www.matthewzeiler.com/mattzeiler/deconvolutionalnetworks.pdf\\n    ')```", output_padding: _size_2_t=0, device='```(None)```', padding: _size_2_t=0, out_channels: int, groups: int=1, dilation: _size_2_t=1, in_channels: int, bias: bool=True, padding_mode: str='zeros', dtype='```(None)```', kernel_size: _size_2_t, stride: _size_2_t=1, transposed: _empty) -> NoneType` | Applies a 2D transposed convolution operator over an input image composed of several input planes. |
| [ ] | torch | torch.nn | ConvTranspose3d | torch.nn.ConvTranspose3d | `(__doc__="```('Applies a 3D transposed convolution operator over an input image composed of several input\\n    planes.\\n    The transposed convolution operator multiplies each input value element-wise by a learnable kernel,\\n    and sums over the outputs from all input feature planes.\\n\\n    This module can be seen as the gradient of Conv3d with respect to its input.\\n    It is also known as a fractionally-strided convolution or\\n    a deconvolution (although it is not an actual deconvolution operation as it does\\n    not compute a true inverse of convolution). For more information, see the visualizations\\n    `here`_ and the `Deconvolutional Networks`_ paper.\\n\\n    This module supports :ref:`TensorFloat32<tf32_on_ampere>`.\\n\\n    On certain ROCm devices, when using float16 inputs this module will use :ref:`different precision<fp16_on_mi200>` for backward.\\n\\n    * :attr:`stride` controls the stride for the cross-correlation.\\n\\n    * :attr:`padding` controls the amount of implicit zero padding on both\\n      sides for ``dilation * (kernel_size - 1) - padding`` number of points. See note\\n      below for details.\\n\\n    * :attr:`output_padding` controls the additional size added to one side\\n      of the output shape. See note below for details.\\n\\n    * :attr:`dilation` controls the spacing between the kernel points; also known as the à trous algorithm.\\n      It is harder to describe, but the link `here`_ has a nice visualization of what :attr:`dilation` does.\\n\\n    {groups_note}\\n\\n    The parameters :attr:`kernel_size`, :attr:`stride`, :attr:`padding`, :attr:`output_padding`\\n    can either be:\\n\\n        - a single ``int`` -- in which case the same value is used for the depth, height and width dimensions\\n        - a ``tuple`` of three ints -- in which case, the first `int` is used for the depth dimension,\\n          the second `int` for the height dimension and the third `int` for the width dimension\\n\\n    Note:\\n        The :attr:`padding` argument effectively adds ``dilation * (kernel_size - 1) - padding``\\n        amount of zero padding to both sizes of the input. This is set so that\\n        when a :class:`~torch.nn.Conv3d` and a :class:`~torch.nn.ConvTranspose3d`\\n        are initialized with same parameters, they are inverses of each other in\\n        regard to the input and output shapes. However, when ``stride > 1``,\\n        :class:`~torch.nn.Conv3d` maps multiple input shapes to the same output\\n        shape. :attr:`output_padding` is provided to resolve this ambiguity by\\n        effectively increasing the calculated output shape on one side. Note\\n        that :attr:`output_padding` is only used to find output shape, but does\\n        not actually add zero-padding to output.\\n\\n    Note:\\n        {cudnn_reproducibility_note}\\n\\n    Args:\\n        in_channels (int): Number of channels in the input image\\n        out_channels (int): Number of channels produced by the convolution\\n        kernel_size (int or tuple): Size of the convolving kernel\\n        stride (int or tuple, optional): Stride of the convolution. Default: 1\\n        padding (int or tuple, optional): ``dilation * (kernel_size - 1) - padding`` zero-padding\\n            will be added to both sides of each dimension in the input. Default: 0\\n        output_padding (int or tuple, optional): Additional size added to one side\\n            of each dimension in the output shape. Default: 0\\n        groups (int, optional): Number of blocked connections from input channels to output channels. Default: 1\\n        bias (bool, optional): If ``True``, adds a learnable bias to the output. Default: ``True``\\n        dilation (int or tuple, optional): Spacing between kernel elements. Default: 1\\n    '.format(**reproducibility_notes, **convolution_notes) + '\\n\\n    Shape:\\n        - Input: :math:`(N, C_{in}, D_{in}, H_{in}, W_{in})` or :math:`(C_{in}, D_{in}, H_{in}, W_{in})`\\n        - Output: :math:`(N, C_{out}, D_{out}, H_{out}, W_{out})` or\\n          :math:`(C_{out}, D_{out}, H_{out}, W_{out})`, where\\n\\n        .. math::\\n              D_{out} = (D_{in} - 1) \\\\times \\\\text{stride}[0] - 2 \\\\times \\\\text{padding}[0] + \\\\text{dilation}[0]\\n                        \\\\times (\\\\text{kernel\\\\_size}[0] - 1) + \\\\text{output\\\\_padding}[0] + 1\\n        .. math::\\n              H_{out} = (H_{in} - 1) \\\\times \\\\text{stride}[1] - 2 \\\\times \\\\text{padding}[1] + \\\\text{dilation}[1]\\n                        \\\\times (\\\\text{kernel\\\\_size}[1] - 1) + \\\\text{output\\\\_padding}[1] + 1\\n        .. math::\\n              W_{out} = (W_{in} - 1) \\\\times \\\\text{stride}[2] - 2 \\\\times \\\\text{padding}[2] + \\\\text{dilation}[2]\\n                        \\\\times (\\\\text{kernel\\\\_size}[2] - 1) + \\\\text{output\\\\_padding}[2] + 1\\n\\n\\n    Attributes:\\n        weight (Tensor): the learnable weights of the module of shape\\n                         :math:`(\\\\text{in\\\\_channels}, \\\\frac{\\\\text{out\\\\_channels}}{\\\\text{groups}},`\\n                         :math:`\\\\text{kernel\\\\_size[0]}, \\\\text{kernel\\\\_size[1]}, \\\\text{kernel\\\\_size[2]})`.\\n                         The values of these weights are sampled from\\n                         :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{out} * \\\\prod_{i=0}^{2}\\\\text{kernel\\\\_size}[i]}`\\n        bias (Tensor):   the learnable bias of the module of shape (out_channels)\\n                         If :attr:`bias` is ``True``, then the values of these weights are\\n                         sampled from :math:`\\\\mathcal{U}(-\\\\sqrt{k}, \\\\sqrt{k})` where\\n                         :math:`k = \\\\frac{groups}{C_\\\\text{out} * \\\\prod_{i=0}^{2}\\\\text{kernel\\\\_size}[i]}`\\n\\n    Examples::\\n\\n        >>> # With square kernels and equal stride\\n        >>> m = nn.ConvTranspose3d(16, 33, 3, stride=2)\\n        >>> # non-square kernels and unequal stride and with padding\\n        >>> m = nn.ConvTranspose3d(16, 33, (3, 5, 2), stride=(2, 1, 1), padding=(0, 4, 2))\\n        >>> input = torch.randn(20, 16, 10, 50, 100)\\n        >>> output = m(input)\\n\\n    .. _`here`:\\n        https://github.com/vdumoulin/conv_arithmetic/blob/master/README.md\\n\\n    .. _`Deconvolutional Networks`:\\n        https://www.matthewzeiler.com/mattzeiler/deconvolutionalnetworks.pdf\\n    ')```", output_padding: _size_3_t=0, device='```(None)```', padding: _size_3_t=0, out_channels: int, groups: int=1, dilation: _size_3_t=1, in_channels: int, bias: bool=True, padding_mode: str='zeros', dtype='```(None)```', kernel_size: _size_3_t, stride: _size_3_t=1, transposed: _empty) -> NoneType` | Applies a 3D transposed convolution operator over an input image composed of several input planes... |
| [ ] | torch | torch.nn | CosineEmbeddingLoss | torch.nn.CosineEmbeddingLoss | `(margin: float=':math:`0', size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['margin', 'reduction']) -> NoneType` | Creates a criterion that measures the loss given input tensors :math:`x_1`, :math:`x_2` and a `Te... |
| [ ] | torch | torch.nn | CosineSimilarity | torch.nn.CosineSimilarity | `(dim: int=1, eps: float=1e-08, __constants__: list=['dim', 'eps']) -> NoneType` | Returns cosine similarity between :math:`x_1` and :math:`x_2`, computed along `dim`. |
| [ ] | torch | torch.nn | CrossEntropyLoss | torch.nn.CrossEntropyLoss | `(weight: Tensor | None='```(None)```', size_average: bool | None=True, ignore_index: int=-100, reduce: bool | None=True, reduction: str | None='mean', label_smoothing: float=':math:`0.0', __constants__: list=['ignore_index', 'reduction', 'label_smoothing']) -> NoneType` | This criterion computes the cross entropy loss between input logits and target. |
| [ ] | torch | torch.nn | CrossMapLRN2d | torch.nn.CrossMapLRN2d | `(size: int, alpha: float=0.0001, beta: float=0.75, k: float=1) -> NoneType` | Base class for all neural network modules. |
| [ ] | torch | torch.nn | DataParallel | torch.nn.DataParallel | `(module: ~T, device_ids: collections.abc.Sequence[int | torch.device] | None=None, output_device: int | torch.device | NoneType=None, dim: int=0) -> NoneType` | Implements data parallelism at the module level. |
| [ ] | torch | torch.nn | Dropout | torch.nn.Dropout | `(p: float=0.5, inplace: bool=False) -> NoneType` | During training, randomly zeroes some of the elements of the input tensor with probability :attr:... |
| [ ] | torch | torch.nn | Dropout1d | torch.nn.Dropout1d | `(p: float | None, inplace: bool | None) -> NoneType` | Randomly zero out entire channels. |
| [ ] | torch | torch.nn | Dropout2d | torch.nn.Dropout2d | `(p: float | None, inplace: bool | None) -> NoneType` | Randomly zero out entire channels. |
| [ ] | torch | torch.nn | Dropout3d | torch.nn.Dropout3d | `(p: float | None, inplace: bool | None) -> NoneType` | Randomly zero out entire channels. |
| [ ] | torch | torch.nn | ELU | torch.nn.ELU | `(alpha: float=1.0, inplace: bool=False, __constants__: list=['alpha', 'inplace']) -> NoneType` | Applies the Exponential Linear Unit (ELU) function, element-wise. |
| [ ] | torch | torch.nn | Embedding | torch.nn.Embedding | `(num_embeddings: int, embedding_dim: int, padding_idx: int | None='```(None)```', max_norm: float | None='```(None)```', norm_type: float=2.0, scale_grad_by_freq: bool=False, sparse: bool=False, __constants__: list=['num_embeddings', 'embedding_dim', 'padding_idx', 'max_norm', 'norm_type', 'scale_grad_by_freq', 'sparse'], weight: Tensor, freeze: bool, device='```(None)```', _freeze: bool=False, dtype='```(None)```', _weight: Tensor | None='```(None)```') -> NoneType` | A simple lookup table that stores embeddings of a fixed dictionary and size. |
| [ ] | torch | torch.nn | EmbeddingBag | torch.nn.EmbeddingBag | `(num_embeddings: int, embedding_dim: int, max_norm: float | None='```(None)```', norm_type: float=2.0, scale_grad_by_freq: bool=False, mode: str='mean', sparse: bool=False, include_last_offset: bool=False, padding_idx: int | None='```(None)```', __constants__: list=['num_embeddings', 'embedding_dim', 'max_norm', 'norm_type', 'scale_grad_by_freq', 'mode', 'sparse', 'include_last_offset', 'padding_idx'], weight: Tensor, device='```(None)```', _weight: Tensor | None='```(None)```', dtype='```(None)```') -> NoneType` | Compute sums or means of 'bags' of embeddings, without instantiating the intermediate embeddings. |
| [ ] | torch | torch.nn | FeatureAlphaDropout | torch.nn.FeatureAlphaDropout | `(p: float | None=0.5, inplace: bool | None='```(None)```') -> NoneType` | Randomly masks out entire channels. |
| [ ] | torch | torch.nn | Flatten | torch.nn.Flatten | `(start_dim: int=1, end_dim: int=-1, __constants__: list=['start_dim', 'end_dim']) -> NoneType` | Flattens a contiguous range of dims into a tensor. |
| [ ] | torch | torch.nn | Fold | torch.nn.Fold | `(output_size: _size_any_t, kernel_size: _size_any_t, dilation: _size_any_t=1, padding: _size_any_t=0, stride: _size_any_t=1, __constants__: list=['output_size', 'kernel_size', 'dilation', 'padding', 'stride']) -> NoneType` | Combines an array of sliding local blocks into a large containing tensor. |
| [ ] | torch | torch.nn | FractionalMaxPool2d | torch.nn.FractionalMaxPool2d | `(kernel_size: _size_2_t, output_size: _size_2_t='```(None)```', output_ratio: _ratio_2_t='```(None)```', return_indices: bool=False, __constants__: list=['kernel_size', 'return_indices', 'output_size', 'output_ratio'], _random_samples='```(None)```') -> NoneType` | Applies a 2D fractional max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | FractionalMaxPool3d | torch.nn.FractionalMaxPool3d | `(kernel_size: _size_3_t, output_size: _size_3_t='```(None)```', output_ratio: _ratio_3_t='```(None)```', return_indices: bool=False, __constants__: list=['kernel_size', 'return_indices', 'output_size', 'output_ratio'], _random_samples='```(None)```') -> NoneType` | Applies a 3D fractional max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | GELU | torch.nn.GELU | `(approximate: str='none', __constants__: list=['approximate']) -> NoneType` | Applies the Gaussian Error Linear Units function. |
| [ ] | torch | torch.nn | GLU | torch.nn.GLU | `(dim: int=-1, __constants__: list=['dim']) -> NoneType` | Applies the gated linear unit function. |
| [ ] | torch | torch.nn | GRU | torch.nn.GRU | `(input_size: int, hidden_size: int, num_layers: int=1, bias: bool=True, batch_first: bool=False, dropout: int=0, bidirectional: bool=False, device='```(None)```', dtype='```(None)```', args=(), kwargs={}, mode: str, proj_size: int=0)` | __init__(input_size,hidden_size,num_layers=1,bias=True,batch_first=False,dropout=0.0,bidirectiona... |
| [ ] | torch | torch.nn | GRUCell | torch.nn.GRUCell | `(input_size: int, hidden_size: int, bias: bool=True, device='```(None)```', dtype='```(None)```', num_chunks: int) -> NoneType` | A gated recurrent unit (GRU) cell. |
| [ ] | torch | torch.nn | GaussianNLLLoss | torch.nn.GaussianNLLLoss | `(full: bool=False, eps: float=1e-06, reduction: str | None='mean', __constants__: list=['full', 'eps', 'reduction']) -> NoneType` | Gaussian negative log likelihood loss. |
| [ ] | torch | torch.nn | GroupNorm | torch.nn.GroupNorm | `(num_groups: int, num_channels: int, eps: float=1e-05, affine: bool=True, __constants__: list=['num_groups', 'num_channels', 'eps', 'affine'], device='```(None)```', dtype='```(None)```') -> NoneType` | Applies Group Normalization over a mini-batch of inputs. |
| [ ] | torch | torch.nn | Hardshrink | torch.nn.Hardshrink | `(lambd: float=0.5, __constants__: list=['lambd']) -> NoneType` | Applies the Hard Shrinkage (Hardshrink) function element-wise. |
| [ ] | torch | torch.nn | Hardsigmoid | torch.nn.Hardsigmoid | `(inplace: bool=False, __constants__: list=['inplace']) -> NoneType` | Applies the Hardsigmoid function element-wise. |
| [ ] | torch | torch.nn | Hardswish | torch.nn.Hardswish | `(inplace: bool=False, __constants__: list=['inplace']) -> NoneType` | Applies the Hardswish function, element-wise. |
| [ ] | torch | torch.nn | Hardtanh | torch.nn.Hardtanh | `(min_val: float=-1.0, max_val: float=1, inplace: bool=False, __constants__: list=['min_val', 'max_val', 'inplace'], min_value: float | None='```(None)```', max_value: float | None='```(None)```') -> NoneType` | Applies the HardTanh function element-wise. |
| [ ] | torch | torch.nn | HingeEmbeddingLoss | torch.nn.HingeEmbeddingLoss | `(margin: float=1.0, size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['margin', 'reduction']) -> NoneType` | Measures the loss given an input tensor :math:`x` and a labels tensor :math:`y` (containing 1 or ... |
| [ ] | torch | torch.nn | HuberLoss | torch.nn.HuberLoss | `(reduction: str | None='mean', delta: float | None=1.0, __constants__: list=['reduction', 'delta']) -> NoneType` | Creates a criterion that uses a squared term if the absolute element-wise error falls below delta... |
| [ ] | torch | torch.nn | Identity | torch.nn.Identity | `(args: Any, kwargs: dict | None) -> NoneType` | A placeholder identity operator that is argument-insensitive. |
| [ ] | torch | torch.nn | InstanceNorm1d | torch.nn.InstanceNorm1d | `(num_features: int, eps: float=1e-05, momentum: float=0.1, affine: bool=False, track_running_stats: bool=False, device=None, dtype=None) -> NoneType` | Applies Instance Normalization. |
| [ ] | torch | torch.nn | InstanceNorm2d | torch.nn.InstanceNorm2d | `(num_features: int, eps: float=1e-05, momentum: float=0.1, affine: bool=False, track_running_stats: bool=False, device=None, dtype=None) -> NoneType` | Applies Instance Normalization. |
| [ ] | torch | torch.nn | InstanceNorm3d | torch.nn.InstanceNorm3d | `(num_features: int, eps: float=1e-05, momentum: float=0.1, affine: bool=False, track_running_stats: bool=False, device=None, dtype=None) -> NoneType` | Applies Instance Normalization. |
| [ ] | torch | torch.nn | KLDivLoss | torch.nn.KLDivLoss | `(size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', log_target: bool | None=False, __constants__: list=['reduction']) -> NoneType` | The Kullback-Leibler divergence loss. |
| [ ] | torch | torch.nn | L1Loss | torch.nn.L1Loss | `(size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['reduction']) -> NoneType` | Creates a criterion that measures the mean absolute error (MAE) between each element in the input... |
| [ ] | torch | torch.nn | LPPool1d | torch.nn.LPPool1d | `(kernel_size: _size_1_t, stride: _size_1_t=':attr:`kernel_size', ceil_mode: bool='```(None)```', norm_type: float) -> NoneType` | Applies a 1D power-average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | LPPool2d | torch.nn.LPPool2d | `(kernel_size: _size_2_t, stride: _size_2_t=':attr:`kernel_size', ceil_mode: bool='```(None)```', norm_type: float) -> NoneType` | Applies a 2D power-average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | LPPool3d | torch.nn.LPPool3d | `(kernel_size: _size_3_t, stride: _size_3_t=':attr:`kernel_size', ceil_mode: bool='```(None)```', norm_type: float) -> NoneType` | Applies a 3D power-average pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | LSTM | torch.nn.LSTM | `(input_size: int, hidden_size: int, num_layers: int=1, bias: bool=True, batch_first: bool=False, dropout: int=0, bidirectional: bool=False, proj_size: int=0, device='```(None)```', dtype='```(None)```', args=(), kwargs={}, mode: str)` | __init__(input_size,hidden_size,num_layers=1,bias=True,batch_first=False,dropout=0.0,bidirectiona... |
| [ ] | torch | torch.nn | LSTMCell | torch.nn.LSTMCell | `(input_size: int, hidden_size: int, bias: bool=True, device='```(None)```', dtype='```(None)```', num_chunks: int) -> NoneType` | A long short-term memory (LSTM) cell. |
| [ ] | torch | torch.nn | LayerNorm | torch.nn.LayerNorm | `(normalized_shape: tuple[int, ...], __constants__: list=['normalized_shape', 'eps', 'elementwise_affine'], eps: float=1e-05, elementwise_affine: bool=True, device='```(None)```', bias: bool=True, dtype='```(None)```') -> NoneType` | Applies Layer Normalization over a mini-batch of inputs. |
| [ ] | torch | torch.nn | LazyBatchNorm1d | torch.nn.LazyBatchNorm1d | `(eps: float=1e-05, momentum: float=0.1, affine: bool=True, track_running_stats: bool=True, cls_to_become: str='BatchNorm1d', device=None, dtype=None) -> NoneType` | A :class:`torch.nn.BatchNorm1d` module with lazy initialization. |
| [ ] | torch | torch.nn | LazyBatchNorm2d | torch.nn.LazyBatchNorm2d | `(eps: float=1e-05, momentum: float=0.1, affine: bool=True, track_running_stats: bool=True, cls_to_become: str='BatchNorm2d', device=None, dtype=None) -> NoneType` | A :class:`torch.nn.BatchNorm2d` module with lazy initialization. |
| [ ] | torch | torch.nn | LazyBatchNorm3d | torch.nn.LazyBatchNorm3d | `(eps: float=1e-05, momentum: float=0.1, affine: bool=True, track_running_stats: bool=True, cls_to_become: str='BatchNorm3d', device=None, dtype=None) -> NoneType` | A :class:`torch.nn.BatchNorm3d` module with lazy initialization. |
| [ ] | torch | torch.nn | LazyConv1d | torch.nn.LazyConv1d | `(out_channels: int, kernel_size: int | tuple, stride: int | tuple | optional=1, padding: int | tuple | optional=0, dilation: int | tuple | optional=1, groups: int | None=1, bias: bool | None=True, padding_mode: str | None='zeros', cls_to_become: str='Conv1d', device='```(None)```', dtype='```(None)```', in_channels: int, transposed: bool, output_padding: tuple) -> NoneType` | A :class:`torch.nn.Conv1d` module with lazy initialization of the ``in_channels`` argument. |
| [ ] | torch | torch.nn | LazyConv2d | torch.nn.LazyConv2d | `(out_channels: int, kernel_size: int | tuple, stride: int | tuple | optional=1, padding: int | tuple | optional=0, dilation: int | tuple | optional=1, groups: int | None=1, bias: bool | None=True, padding_mode: str | None='zeros', cls_to_become: str='Conv2d', device='```(None)```', dtype='```(None)```', in_channels: int, transposed: bool, output_padding: tuple) -> NoneType` | A :class:`torch.nn.Conv2d` module with lazy initialization of the ``in_channels`` argument. |
| [ ] | torch | torch.nn | LazyConv3d | torch.nn.LazyConv3d | `(out_channels: int, kernel_size: int | tuple, stride: int | tuple | optional=1, padding: int | tuple | optional=0, dilation: int | tuple | optional=1, groups: int | None=1, bias: bool | None=True, padding_mode: str | None='zeros', cls_to_become: str='Conv3d', device='```(None)```', dtype='```(None)```', in_channels: int, transposed: bool, output_padding: tuple) -> NoneType` | A :class:`torch.nn.Conv3d` module with lazy initialization of the ``in_channels`` argument. |
| [ ] | torch | torch.nn | LazyConvTranspose1d | torch.nn.LazyConvTranspose1d | `(out_channels: int, kernel_size: int | tuple, stride: int | tuple | optional=1, padding: int | tuple | optional=0, output_padding: int | tuple | optional=0, groups: int | None=1, bias: bool | None=True, dilation: int | tuple | optional=1, cls_to_become: str='ConvTranspose1d', dtype='```(None)```', device='```(None)```', padding_mode: str='zeros', in_channels: int, transposed: _empty) -> NoneType` | A :class:`torch.nn.ConvTranspose1d` module with lazy initialization of the ``in_channels`` argument. |
| [ ] | torch | torch.nn | LazyConvTranspose2d | torch.nn.LazyConvTranspose2d | `(out_channels: int, kernel_size: int | tuple, stride: int | tuple | optional=1, padding: int | tuple | optional=0, output_padding: int | tuple | optional=0, groups: int | None=1, bias: bool | None=True, dilation: int | tuple | optional=1, cls_to_become: str='ConvTranspose2d', dtype='```(None)```', device='```(None)```', padding_mode: str='zeros', in_channels: int, transposed: _empty) -> NoneType` | A :class:`torch.nn.ConvTranspose2d` module with lazy initialization of the ``in_channels`` argument. |
| [ ] | torch | torch.nn | LazyConvTranspose3d | torch.nn.LazyConvTranspose3d | `(out_channels: int, kernel_size: int | tuple, stride: int | tuple | optional=1, padding: int | tuple | optional=0, output_padding: int | tuple | optional=0, groups: int | None=1, bias: bool | None=True, dilation: int | tuple | optional=1, cls_to_become: str='ConvTranspose3d', dtype='```(None)```', device='```(None)```', padding_mode: str='zeros', in_channels: int, transposed: _empty) -> NoneType` | A :class:`torch.nn.ConvTranspose3d` module with lazy initialization of the ``in_channels`` argument. |
| [ ] | torch | torch.nn | LazyInstanceNorm1d | torch.nn.LazyInstanceNorm1d | `(num_features, eps: float=1e-05, momentum: float=0.1, affine: bool=False, track_running_stats: bool=False, cls_to_become: str='InstanceNorm1d', device=None, dtype=None) -> NoneType` | A :class:`torch.nn.InstanceNorm1d` module with lazy initialization of the ``num_features`` argument. |
| [ ] | torch | torch.nn | LazyInstanceNorm2d | torch.nn.LazyInstanceNorm2d | `(num_features, eps: float=1e-05, momentum: float=0.1, affine: bool=False, track_running_stats: bool=False, cls_to_become: str='InstanceNorm2d', device=None, dtype=None) -> NoneType` | A :class:`torch.nn.InstanceNorm2d` module with lazy initialization of the ``num_features`` argument. |
| [ ] | torch | torch.nn | LazyInstanceNorm3d | torch.nn.LazyInstanceNorm3d | `(num_features, eps: float=1e-05, momentum: float=0.1, affine: bool=False, track_running_stats: bool=False, cls_to_become: str='InstanceNorm3d', device=None, dtype=None) -> NoneType` | A :class:`torch.nn.InstanceNorm3d` module with lazy initialization of the ``num_features`` argument. |
| [ ] | torch | torch.nn | LazyLinear | torch.nn.LazyLinear | `(out_features: int, bias: UninitializedParameter=True, cls_to_become: str='Linear', weight: UninitializedParameter, device='```(None)```', dtype='```(None)```') -> NoneType` | A :class:`torch.nn.Linear` module where `in_features` is inferred. |
| [ ] | torch | torch.nn | LeakyReLU | torch.nn.LeakyReLU | `(negative_slope: float=0.01, inplace: bool=False, __constants__: list=['inplace', 'negative_slope']) -> NoneType` | Applies the LeakyReLU function element-wise. |
| [ ] | torch | torch.nn | Linear | torch.nn.Linear | `(in_features: int, out_features: int, bias: bool=True, __constants__: list=['in_features', 'out_features'], weight: Tensor, device='```(None)```', dtype='```(None)```') -> NoneType` | Applies an affine linear transformation to the incoming data: :math:`y = xA^T + b`. |
| [ ] | torch | torch.nn | LocalResponseNorm | torch.nn.LocalResponseNorm | `(size: int, alpha: float=0.0001, beta: float=0.75, k: float=1, __constants__: list=['size', 'alpha', 'beta', 'k']) -> NoneType` | Applies local response normalization over an input signal. |
| [ ] | torch | torch.nn | LogSigmoid | torch.nn.LogSigmoid | `(args=(), kwargs={}) -> NoneType` | Applies the Logsigmoid function element-wise. |
| [ ] | torch | torch.nn | LogSoftmax | torch.nn.LogSoftmax | `(dim: int | None='```(None)```', __constants__: list=['dim']) -> NoneType` | Applies the :math:`\log(\text{Softmax}(x))` function to an n-dimensional input Tensor. |
| [ ] | torch | torch.nn | MSELoss | torch.nn.MSELoss | `(size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['reduction']) -> NoneType` | Creates a criterion that measures the mean squared error (squared L2 norm) between each element i... |
| [ ] | torch | torch.nn | MarginRankingLoss | torch.nn.MarginRankingLoss | `(margin: float=0.0, size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['margin', 'reduction']) -> NoneType` | Creates a criterion that measures the loss given inputs :math:`x1`, :math:`x2`, two 1D mini-batch... |
| [ ] | torch | torch.nn | MaxPool1d | torch.nn.MaxPool1d | `(kernel_size: _size_1_t, stride: _size_1_t=':attr:`kernel_size', padding: _size_1_t='```(None)```', dilation: _size_1_t='```(None)```', return_indices: bool='```(None)```', ceil_mode: bool='```(None)```') -> NoneType` | Applies a 1D max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | MaxPool2d | torch.nn.MaxPool2d | `(kernel_size: _size_2_t, stride: _size_2_t=':attr:`kernel_size', padding: _size_2_t='```(None)```', dilation: _size_2_t='```(None)```', return_indices: bool='```(None)```', ceil_mode: bool='```(None)```') -> NoneType` | Applies a 2D max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | MaxPool3d | torch.nn.MaxPool3d | `(kernel_size: _size_3_t, stride: _size_3_t=':attr:`kernel_size', padding: _size_3_t='```(None)```', dilation: _size_3_t='```(None)```', return_indices: bool='```(None)```', ceil_mode: bool='```(None)```') -> NoneType` | Applies a 3D max pooling over an input signal composed of several input planes. |
| [ ] | torch | torch.nn | MaxUnpool1d | torch.nn.MaxUnpool1d | `(kernel_size: _size_1_t, stride: _size_1_t='```(None)```', padding: _size_1_t=0) -> NoneType` | Computes a partial inverse of :class:`MaxPool1d`. |
| [ ] | torch | torch.nn | MaxUnpool2d | torch.nn.MaxUnpool2d | `(kernel_size: _size_2_t, stride: _size_2_t='```(None)```', padding: _size_2_t=0) -> NoneType` | Computes a partial inverse of :class:`MaxPool2d`. |
| [ ] | torch | torch.nn | MaxUnpool3d | torch.nn.MaxUnpool3d | `(kernel_size: _size_3_t, stride: _size_3_t='```(None)```', padding: _size_3_t=0) -> NoneType` | Computes a partial inverse of :class:`MaxPool3d`. |
| [ ] | torch | torch.nn | Mish | torch.nn.Mish | `(__constants__: list=['inplace'], inplace: bool=False)` | Applies the Mish function, element-wise. |
| [ ] | torch | torch.nn | Module | torch.nn.Module | `(training: bool, dump_patches: bool=False, _version: int=1, _parameters: dict[str, Parameter | None], _buffers: dict[str, Tensor | None], _non_persistent_buffers_set: set[str], _backward_pre_hooks: dict[int, Callable], _backward_hooks: dict[int, Callable], _is_full_backward_hook: bool | None, _forward_hooks: dict[int, Callable], _forward_hooks_with_kwargs: dict[int, bool], _forward_hooks_always_called: dict[int, bool], _forward_pre_hooks: dict[int, Callable], _forward_pre_hooks_with_kwargs: dict[int, bool], _state_dict_hooks: dict[int, Callable], _load_state_dict_pre_hooks: dict[int, Callable], _state_dict_pre_hooks: dict[int, Callable], _load_state_dict_post_hooks: dict[int, Callable], _modules: dict[str, 'Module' | None], call_super_init: bool=False, _compiled_call_impl: Callable | None='```(None)```', forward: Callable[..., Any]='_forward_unimplemented', __call__: Callable[..., Any]='_wrapped_call_impl', T_destination="```(TypeVar('T_destination', bound=dict[str, Any]))```", args=(), kwargs={}) -> NoneType` | Base class for all neural network modules. |
| [ ] | torch | torch.nn | ModuleDict | torch.nn.ModuleDict | `(modules: iterable | None='```(None)```', _modules: dict[str, Module])` | Holds submodules in a dictionary. |
| [ ] | torch | torch.nn | ModuleList | torch.nn.ModuleList | `(modules: iterable | None='```(None)```', _modules: dict[str, Module])` | Holds submodules in a list. |
| [ ] | torch | torch.nn | MultiLabelMarginLoss | torch.nn.MultiLabelMarginLoss | `(size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['reduction']) -> NoneType` | Creates a criterion that optimizes a multi-class multi-classification hinge loss (margin-based lo... |
| [ ] | torch | torch.nn | MultiLabelSoftMarginLoss | torch.nn.MultiLabelSoftMarginLoss | `(weight: Tensor | None='```(None)```', size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['reduction']) -> NoneType` | Creates a criterion that optimizes a multi-label one-versus-all loss based on max-entropy, betwee... |
| [ ] | torch | torch.nn | MultiMarginLoss | torch.nn.MultiMarginLoss | `(p: int=1, margin: float=1.0, weight: Tensor | None='```(None)```', size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['p', 'margin', 'reduction']) -> NoneType` | Creates a criterion that optimizes a multi-class classification hinge loss (margin-based loss) be... |
| [ ] | torch | torch.nn | MultiheadAttention | torch.nn.MultiheadAttention | `(embed_dim, num_heads, dropout: str='0.0`` (no dropout).', bias: bool=True, add_bias_kv: bool=False, add_zero_attn: bool=False, kdim: str='None`` (uses ``kdim=embed_dim``).', vdim: str='None`` (uses ``vdim=embed_dim``).', batch_first: str='False`` (seq, batch, feature).', __constants__: list=['batch_first'], bias_k: torch.Tensor | None, bias_v: torch.Tensor | None, device='```(None)```', dtype='```(None)```') -> NoneType` | Allows the model to jointly attend to information from different representation subspaces. |
| [ ] | torch | torch.nn | NLLLoss | torch.nn.NLLLoss | `(weight: Tensor | None='```(None)```', size_average: bool | None='```(None)```', ignore_index: int=-100, reduce: bool | None='```(None)```', reduction: str | None='mean', __constants__: list=['ignore_index', 'reduction']) -> NoneType` | The negative log likelihood loss. It is useful to train a classification problem with `C` classes. |
| [ ] | torch | torch.nn | NLLLoss2d | torch.nn.NLLLoss2d | `(weight: Tensor | None='```(None)```', size_average: bool | None='```(None)```', ignore_index: int=-100, reduce: bool | None='```(None)```', reduction: str='mean') -> NoneType` | The negative log likelihood loss. It is useful to train a classification problem with `C` classes. |
| [ ] | torch | torch.nn | PReLU | torch.nn.PReLU | `(num_parameters: int=1, init: float=0.25, __constants__: list=['num_parameters'], device='```(None)```', dtype='```(None)```') -> NoneType` | Applies the element-wise PReLU function. |
| [ ] | torch | torch.nn | PairwiseDistance | torch.nn.PairwiseDistance | `(p: real | None=2, eps: float=1e-06, keepdim: bool=False, __constants__: list=['norm', 'eps', 'keepdim'], norm: float) -> NoneType` | Computes the pairwise distance between input vectors, or between columns of input matrices. |
| [ ] | torch | torch.nn | ParameterDict | torch.nn.ParameterDict | `(values: iterable | None, parameters: Any='```(None)```') -> NoneType` | Holds parameters in a dictionary. |
| [ ] | torch | torch.nn | ParameterList | torch.nn.ParameterList | `(parameters: iterable | None, values: Iterable[Any] | None='```(None)```')` | Holds parameters in a list. |
| [ ] | torch | torch.nn | PixelShuffle | torch.nn.PixelShuffle | `(upscale_factor: int, __constants__: list=['upscale_factor']) -> NoneType` | Rearrange elements in a tensor according to an upscaling factor. |
| [ ] | torch | torch.nn | PixelUnshuffle | torch.nn.PixelUnshuffle | `(downscale_factor: int, __constants__: list=['downscale_factor']) -> NoneType` | Reverse the PixelShuffle operation. |
| [ ] | torch | torch.nn | PoissonNLLLoss | torch.nn.PoissonNLLLoss | `(log_input: bool=True, full: bool=False, __constants__: list=['log_input', 'full', 'eps', 'reduction'], eps: float=1e-08, reduce='```(None)```', reduction: str='mean', size_average='```(None)```') -> NoneType` | Negative log likelihood loss with Poisson distribution of target. |
| [ ] | torch | torch.nn | RMSNorm | torch.nn.RMSNorm | `(normalized_shape: tuple[int, ...], __constants__: list=['normalized_shape', 'eps', 'elementwise_affine'], eps: float | None='```(None)```', elementwise_affine: bool=True, device='```(None)```', dtype='```(None)```') -> NoneType` | Applies Root Mean Square Layer Normalization over a mini-batch of inputs. |
| [ ] | torch | torch.nn | RNN | torch.nn.RNN | `(input_size: int, hidden_size: int, num_layers: int=1, nonlinearity: str='tanh', bias: bool=True, batch_first: bool=False, dropout: int=0, bidirectional: bool=False, device='```(None)```', dtype='```(None)```', args=(), kwargs={}, mode: str, proj_size: int=0)` | __init__(input_size,hidden_size,num_layers=1,nonlinearity='tanh',bias=True,batch_first=False,drop... |
| [ ] | torch | torch.nn | RNNBase | torch.nn.RNNBase | `(__constants__: list=['mode', 'input_size', 'hidden_size', 'num_layers', 'bias', 'batch_first', 'dropout', 'bidirectional', 'proj_size'], __jit_unused_properties__: list=['all_weights'], mode: str, input_size: int, hidden_size: int, num_layers: int=1, bias: bool=True, batch_first: bool=False, dropout: float=0.0, bidirectional: bool=False, proj_size: int=0, device='```(None)```', dtype='```(None)```') -> NoneType` | Base class for RNN modules (RNN, LSTM, GRU). |
| [ ] | torch | torch.nn | RNNCell | torch.nn.RNNCell | `(input_size: int, hidden_size: int, bias: bool=True, nonlinearity: str='tanh', __constants__: list=['input_size', 'hidden_size', 'bias', 'nonlinearity'], device='```(None)```', dtype='```(None)```', num_chunks: int) -> NoneType` | An Elman RNN cell with tanh or ReLU non-linearity. |
| [ ] | torch | torch.nn | RNNCellBase | torch.nn.RNNCellBase | `(__constants__: list=['input_size', 'hidden_size', 'bias'], input_size: int, hidden_size: int, bias: bool, weight_ih: Tensor, weight_hh: Tensor, device='```(None)```', dtype='```(None)```', num_chunks: int) -> NoneType` | Base class for all neural network modules. |
| [ ] | torch | torch.nn | RReLU | torch.nn.RReLU | `(lower: float=':math:`\\frac{1}{8}', upper: float=':math:`\\frac{1}{3}', inplace: bool=False, __constants__: list=['lower', 'upper', 'inplace'])` | Applies the randomized leaky rectified linear unit function, element-wise. |
| [ ] | torch | torch.nn | ReLU | torch.nn.ReLU | `(inplace: bool=False, __constants__: list=['inplace'])` | Applies the rectified linear unit function element-wise. |
| [ ] | torch | torch.nn | ReLU6 | torch.nn.ReLU6 | `(inplace: bool=False)` | Applies the ReLU6 function element-wise. |
| [ ] | torch | torch.nn | ReflectionPad1d | torch.nn.ReflectionPad1d | `(padding: tuple[int, int]) -> NoneType` | Pads the input tensor using the reflection of the input boundary. |
| [ ] | torch | torch.nn | ReflectionPad2d | torch.nn.ReflectionPad2d | `(padding: tuple[int, int, int, int]) -> NoneType` | Pads the input tensor using the reflection of the input boundary. |
| [ ] | torch | torch.nn | ReflectionPad3d | torch.nn.ReflectionPad3d | `(padding: tuple[int, int, int, int, int, int]) -> NoneType` | Pads the input tensor using the reflection of the input boundary. |
| [ ] | torch | torch.nn | ReplicationPad1d | torch.nn.ReplicationPad1d | `(padding: tuple[int, int]) -> NoneType` | Pads the input tensor using replication of the input boundary. |
| [ ] | torch | torch.nn | ReplicationPad2d | torch.nn.ReplicationPad2d | `(padding: tuple[int, int, int, int]) -> NoneType` | Pads the input tensor using replication of the input boundary. |
| [ ] | torch | torch.nn | ReplicationPad3d | torch.nn.ReplicationPad3d | `(padding: tuple[int, int, int, int, int, int]) -> NoneType` | Pads the input tensor using replication of the input boundary. |
| [ ] | torch | torch.nn | SELU | torch.nn.SELU | `(inplace: bool=False, __constants__: list=['inplace']) -> NoneType` | Applies the SELU function element-wise. |
| [ ] | torch | torch.nn | Sequential | torch.nn.Sequential | `(_modules: dict[str, Module], args=())` | A sequential container. |
| [ ] | torch | torch.nn | SiLU | torch.nn.SiLU | `(__constants__: list=['inplace'], inplace: bool=False)` | Applies the Sigmoid Linear Unit (SiLU) function, element-wise. |
| [ ] | torch | torch.nn | Sigmoid | torch.nn.Sigmoid | `(args=(), kwargs={}) -> NoneType` | Applies the Sigmoid function element-wise. |
| [ ] | torch | torch.nn | SmoothL1Loss | torch.nn.SmoothL1Loss | `(size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', beta: float | None=1.0, __constants__: list=['reduction']) -> NoneType` | Creates a criterion that uses a squared term if the absolute element-wise error falls below beta ... |
| [ ] | torch | torch.nn | SoftMarginLoss | torch.nn.SoftMarginLoss | `(size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['reduction']) -> NoneType` | Creates a criterion that optimizes a two-class classification logistic loss between input tensor ... |
| [ ] | torch | torch.nn | Softmax | torch.nn.Softmax | `(dim: int | None='```(None)```', __constants__: list=['dim']) -> NoneType` | Applies the Softmax function to an n-dimensional input Tensor. |
| [ ] | torch | torch.nn | Softmax2d | torch.nn.Softmax2d | `(args=(), kwargs={}) -> NoneType` | Applies SoftMax over features to each spatial location. |
| [ ] | torch | torch.nn | Softmin | torch.nn.Softmin | `(dim: int | None='```(None)```', __constants__: list=['dim']) -> NoneType` | Applies the Softmin function to an n-dimensional input Tensor. |
| [ ] | torch | torch.nn | Softplus | torch.nn.Softplus | `(beta: float=1, threshold: float=20, __constants__: list=['beta', 'threshold']) -> NoneType` | Applies the Softplus function element-wise. |
| [ ] | torch | torch.nn | Softshrink | torch.nn.Softshrink | `(lambd: float=0.5, __constants__: list=['lambd']) -> NoneType` | Applies the soft shrinkage function element-wise. |
| [ ] | torch | torch.nn | Softsign | torch.nn.Softsign | `(args=(), kwargs={}) -> NoneType` | Applies the element-wise Softsign function. |
| [ ] | torch | torch.nn | SyncBatchNorm | torch.nn.SyncBatchNorm | `(num_features: int, eps: float=1e-05, momentum: float | None=0.1, affine: bool=True, track_running_stats: bool=True, process_group: Any | None='```(None)```', device='```(None)```', dtype='```(None)```') -> NoneType` | Applies Batch Normalization over a N-Dimensional input. |
| [ ] | torch | torch.nn | Tanh | torch.nn.Tanh | `(args=(), kwargs={}) -> NoneType` | Applies the Hyperbolic Tangent (Tanh) function element-wise. |
| [ ] | torch | torch.nn | Tanhshrink | torch.nn.Tanhshrink | `(args=(), kwargs={}) -> NoneType` | Applies the element-wise Tanhshrink function. |
| [ ] | torch | torch.nn | Threshold | torch.nn.Threshold | `(threshold: float, value: float, inplace: bool=False, __constants__: list=['threshold', 'value', 'inplace']) -> NoneType` | Thresholds each element of the input Tensor. |
| [ ] | torch | torch.nn | Transformer | torch.nn.Transformer | `(d_model: int=512, nhead: int=8, num_encoder_layers: int=6, num_decoder_layers: int=6, dim_feedforward: int=2048, dropout: float=0.1, activation: str | Callable[[Tensor], Tensor]='relu', custom_encoder: Any | None='```(None)```', custom_decoder: Any | None='```(None)```', layer_norm_eps: float=1e-05, batch_first: str='False`` (seq, batch, feature).', norm_first: str='False`` (after).', bias: bool=True, device='```(None)```', dtype='```(None)```') -> NoneType` | A basic transformer layer. |
| [ ] | torch | torch.nn | TransformerDecoder | torch.nn.TransformerDecoder | `(decoder_layer: 'TransformerDecoderLayer', num_layers: int, norm: Module | None='```(None)```', __constants__: list=['norm']) -> NoneType` | TransformerDecoder is a stack of N decoder layers. |
| [ ] | torch | torch.nn | TransformerDecoderLayer | torch.nn.TransformerDecoderLayer | `(d_model: int, nhead: int, dim_feedforward: int=2048, dropout: float=0.1, activation: str | Callable[[Tensor], Tensor]='relu', layer_norm_eps: float=1e-05, batch_first: str='False`` (seq, batch, feature).', norm_first: str='False`` (after).', bias: bool=True, __constants__: list=['norm_first'], device='```(None)```', dtype='```(None)```') -> NoneType` | TransformerDecoderLayer is made up of self-attn, multi-head-attn and feedforward network. |
| [ ] | torch | torch.nn | TransformerEncoder | torch.nn.TransformerEncoder | `(encoder_layer: 'TransformerEncoderLayer', num_layers: int, norm: Module | None='```(None)```', enable_nested_tensor: str='True`` (enabled).', __constants__: list=['norm'], mask_check: bool=True) -> NoneType` | TransformerEncoder is a stack of N encoder layers. |
| [ ] | torch | torch.nn | TransformerEncoderLayer | torch.nn.TransformerEncoderLayer | `(d_model: int, nhead: int, dim_feedforward: int=2048, dropout: float=0.1, activation: str | Callable[[Tensor], Tensor]='relu', layer_norm_eps: float=1e-05, batch_first: str='False`` (seq, batch, feature).', norm_first: str='False`` (after).', bias: bool=True, __constants__: list=['norm_first'], device='```(None)```', dtype='```(None)```') -> NoneType` | TransformerEncoderLayer is made up of self-attn and feedforward network. |
| [ ] | torch | torch.nn | TripletMarginLoss | torch.nn.TripletMarginLoss | `(margin: float=':math:`1', p: float=':math:`2', eps: float=':math:`1e-6', swap: bool=False, size_average: bool | None=True, reduce: bool | None=True, reduction: str | None='mean', __constants__: list=['margin', 'p', 'eps', 'swap', 'reduction'])` | Creates a criterion that measures the triplet loss given an input tensors :math:`x1`, :math:`x2`,... |
| [ ] | torch | torch.nn | TripletMarginWithDistanceLoss | torch.nn.TripletMarginWithDistanceLoss | `(distance_function: Callable | None='```(None)```', margin: float=':math:`1', swap: bool=False, reduction: str | None='mean', __constants__: list=['margin', 'swap', 'reduction'])` | Creates a criterion that measures the triplet loss given input tensors :math:`a`, :math:`p`, and ... |
| [ ] | torch | torch.nn | Unflatten | torch.nn.Unflatten | `(dim: int | str, unflattened_size: _size | NamedShape, NamedShape='```(tuple)```', __constants__: list=['dim', 'unflattened_size']) -> NoneType` | Unflattens a tensor dim expanding it to a desired shape. For use with :class:`~nn.Sequential`. |
| [ ] | torch | torch.nn | Unfold | torch.nn.Unfold | `(kernel_size: _size_any_t, dilation: _size_any_t=1, padding: _size_any_t=0, stride: _size_any_t=1, __constants__: list=['kernel_size', 'dilation', 'padding', 'stride']) -> NoneType` | Extracts sliding local blocks from a batched input tensor. |
| [ ] | torch | torch.nn | Upsample | torch.nn.Upsample | `(__constants__: list=['size', 'scale_factor', 'mode', 'align_corners', 'name', 'recompute_scale_factor'], name: str, size: _size_any_t | None='```(None)```', scale_factor: _ratio_any_t | None='```(None)```', mode: str='nearest', align_corners: bool | None='```(None)```', recompute_scale_factor: bool | None='```(None)```') -> NoneType` | Upsamples a given multi-channel 1D (temporal), 2D (spatial) or 3D (volumetric) data. |
| [ ] | torch | torch.nn | UpsamplingBilinear2d | torch.nn.UpsamplingBilinear2d | `(size: int | tuple[int, int] | optional='```(None)```', scale_factor: float | tuple[float, float] | optional='```(None)```') -> NoneType` | Applies a 2D bilinear upsampling to an input signal composed of several input channels. |
| [ ] | torch | torch.nn | UpsamplingNearest2d | torch.nn.UpsamplingNearest2d | `(size: int | tuple[int, int] | optional='```(None)```', scale_factor: float | tuple[float, float] | optional='```(None)```') -> NoneType` | Applies a 2D nearest neighbor upsampling to an input signal composed of several input channels. |
| [ ] | torch | torch.nn | ZeroPad1d | torch.nn.ZeroPad1d | `(padding: tuple[int, int]) -> NoneType` | Pads the input tensor boundaries with zero. |
| [ ] | torch | torch.nn | ZeroPad2d | torch.nn.ZeroPad2d | `(padding: tuple[int, int, int, int]) -> NoneType` | Pads the input tensor boundaries with zero. |
| [ ] | torch | torch.nn | ZeroPad3d | torch.nn.ZeroPad3d | `(padding: tuple[int, int, int, int, int, int]) -> NoneType` | Pads the input tensor boundaries with zero. |
| [ ] | torch | torch.nn.init | calculate_gain | torch.nn.init.calculate_gain | `(nonlinearity: _NonlinearityType, param: _Optional[int | float]='```(None)```') -> float` | Return the recommended gain value for the given nonlinearity function. |
| [ ] | torch | torch.nn.init | constant | torch.nn.init.constant | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | constant(...) |
| [ ] | torch | torch.nn.init | constant_ | torch.nn.init.constant_ | `(tensor: Tensor, val: float) -> torch.Tensor` | Fill the input Tensor with the value :math:`\text{val}`. |
| [ ] | torch | torch.nn.init | dirac | torch.nn.init.dirac | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | dirac(...) |
| [ ] | torch | torch.nn.init | dirac_ | torch.nn.init.dirac_ | `(tensor: Tensor, groups: int | None=1) -> torch.Tensor` | Fill the {3, 4, 5}-dimensional input `Tensor` with the Dirac delta function. |
| [ ] | torch | torch.nn.init | eye | torch.nn.init.eye | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | eye(...) |
| [ ] | torch | torch.nn.init | eye_ | torch.nn.init.eye_ | `(tensor: Tensor) -> torch.Tensor` | Fill the 2-dimensional input `Tensor` with the identity matrix. |
| [ ] | torch | torch.nn.init | kaiming_normal | torch.nn.init.kaiming_normal | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | kaiming_normal(...) |
| [ ] | torch | torch.nn.init | kaiming_normal_ | torch.nn.init.kaiming_normal_ | `(tensor: Tensor, a: float=0, mode: _FanMode='fan_in', nonlinearity: _NonlinearityType='leaky_relu', generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input `Tensor` with values using a Kaiming normal distribution. |
| [ ] | torch | torch.nn.init | kaiming_uniform | torch.nn.init.kaiming_uniform | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | kaiming_uniform(...) |
| [ ] | torch | torch.nn.init | kaiming_uniform_ | torch.nn.init.kaiming_uniform_ | `(tensor: Tensor, a: float=0, mode: _FanMode='fan_in', nonlinearity: _NonlinearityType='leaky_relu', generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input `Tensor` with values using a Kaiming uniform distribution. |
| [ ] | torch | torch.nn.init | normal | torch.nn.init.normal | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | normal(...) |
| [ ] | torch | torch.nn.init | normal_ | torch.nn.init.normal_ | `(tensor: Tensor, mean: float=0.0, std: float=1.0, generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input Tensor with values drawn from the normal distribution. |
| [ ] | torch | torch.nn.init | ones_ | torch.nn.init.ones_ | `(tensor: Tensor) -> torch.Tensor` | Fill the input Tensor with the scalar value `1`. |
| [ ] | torch | torch.nn.init | orthogonal | torch.nn.init.orthogonal | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | orthogonal(...) |
| [ ] | torch | torch.nn.init | orthogonal_ | torch.nn.init.orthogonal_ | `(tensor: Tensor, gain: float=1, generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input `Tensor` with a (semi) orthogonal matrix. |
| [ ] | torch | torch.nn.init | sparse | torch.nn.init.sparse | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | sparse(...) |
| [ ] | torch | torch.nn.init | sparse_ | torch.nn.init.sparse_ | `(tensor: Tensor, sparsity: float, std: float=0.01, generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the 2D input `Tensor` as a sparse matrix. |
| [ ] | torch | torch.nn.init | trunc_normal_ | torch.nn.init.trunc_normal_ | `(tensor: Tensor, mean: float=0.0, std: float=1.0, a: float=-2.0, b: float=2.0, generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input Tensor with values drawn from a truncated normal distribution. |
| [ ] | torch | torch.nn.init | uniform | torch.nn.init.uniform | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | uniform(...) |
| [ ] | torch | torch.nn.init | uniform_ | torch.nn.init.uniform_ | `(tensor: Tensor, a: float=0.0, b: float=1.0, generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input Tensor with values drawn from the uniform distribution. |
| [ ] | torch | torch.nn.init | xavier_normal | torch.nn.init.xavier_normal | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | xavier_normal(...) |
| [ ] | torch | torch.nn.init | xavier_normal_ | torch.nn.init.xavier_normal_ | `(tensor: Tensor, gain: float=1.0, generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input `Tensor` with values using a Xavier normal distribution. |
| [ ] | torch | torch.nn.init | xavier_uniform | torch.nn.init.xavier_uniform | `(args: _P.args, kwargs: _P.kwargs) -> ~_R` | xavier_uniform(...) |
| [ ] | torch | torch.nn.init | xavier_uniform_ | torch.nn.init.xavier_uniform_ | `(tensor: Tensor, gain: float=1.0, generator: _Optional[torch.Generator]='```(None)```') -> torch.Tensor` | Fill the input `Tensor` with values using a Xavier uniform distribution. |
| [ ] | torch | torch.nn.init | zeros_ | torch.nn.init.zeros_ | `(tensor: Tensor) -> torch.Tensor` | Fill the input Tensor with the scalar value `0`. |
| [ ] | torch | torch.optim | ASGD | torch.optim.ASGD | `(params: ParamsT, lr: float | Tensor=0.01, lambd: float=0.0001, alpha: float=0.75, t0: float=1000000.0, weight_decay: float=0, foreach: bool | None='```(None)```', maximize: bool=False, differentiable: bool=False, capturable: bool=False)` | Implements Averaged Stochastic Gradient Descent. |
| [ ] | torch | torch.optim | Adadelta | torch.optim.Adadelta | `(params: ParamsT, lr: float | Tensor=1.0, rho: float=0.9, eps: float=1e-06, weight_decay: float=0, foreach: bool | None='```(None)```', capturable: bool=False, maximize: bool=False, differentiable: bool=False)` | Implements Adadelta algorithm. |
| [ ] | torch | torch.optim | Adafactor | torch.optim.Adafactor | `(params: collections.abc.Iterable[torch.Tensor] | collections.abc.Iterable[dict[str, Any]] | collections.abc.Iterable[tuple[str, torch.Tensor]], lr: float | torch.Tensor=0.01, beta2_decay: float=-0.8, eps: tuple[float | None, float]=(None, 0.001), d: float=1.0, weight_decay: float=0.0, foreach: bool | None=None, maximize: bool=False)` | Implements Adafactor algorithm. |
| [ ] | torch | torch.optim | Adagrad | torch.optim.Adagrad | `(params: ParamsT, lr: float | Tensor=0.01, lr_decay: float=0, weight_decay: float=0, initial_accumulator_value: float=0, eps: float=1e-10, foreach: bool | None='```(None)```', maximize: bool=False, differentiable: bool=False, fused: bool | None='```(None)```')` | Implements Adagrad algorithm. |
| [ ] | torch | torch.optim | Adam | torch.optim.Adam | `(params: ParamsT, lr: float | Tensor=0.001, betas: tuple[float | Tensor, float | Tensor]=(0.9, 0.999), eps: float=1e-08, weight_decay: float=0, amsgrad: bool=False, foreach: bool | None='```(None)```', maximize: bool=False, capturable: bool=False, differentiable: bool=False, fused: bool | None='```(None)```', decoupled_weight_decay: bool=False)` | Implements Adam algorithm. |
| [ ] | torch | torch.optim | AdamW | torch.optim.AdamW | `(params: ParamsT, lr: float | Tensor=0.001, betas: tuple[float | Tensor, float | Tensor]=(0.9, 0.999), eps: float=1e-08, weight_decay: float=0.01, amsgrad: bool=False, maximize: bool=False, foreach: bool | None='```(None)```', capturable: bool=False, differentiable: bool=False, fused: bool | None='```(None)```')` | Implements AdamW algorithm, where weight decay does not accumulate in the momentum nor variance. |
| [ ] | torch | torch.optim | Adamax | torch.optim.Adamax | `(params: ParamsT, lr: float | Tensor=0.002, betas: tuple[float, float]=(0.9, 0.999), eps: float=1e-08, weight_decay: float=0, foreach: bool | None='```(None)```', maximize: bool=False, differentiable: bool=False, capturable: bool=False)` | Implements Adamax algorithm (a variant of Adam based on infinity norm). |
| [ ] | torch | torch.optim | LBFGS | torch.optim.LBFGS | `(params: iterable, lr: float | None=1, max_iter: int | None=20, max_eval: int | None='max_iter * 1.25', tolerance_grad: float | None=1e-07, tolerance_change: float | None=1e-09, history_size: int | None=100, line_search_fn: str | None='```(None)```')` | Implements L-BFGS algorithm. |
| [ ] | torch | torch.optim | NAdam | torch.optim.NAdam | `(params: ParamsT, lr: float | Tensor=0.002, betas: tuple[float, float]=(0.9, 0.999), eps: float=1e-08, weight_decay: float=0, momentum_decay: float=0.004, decoupled_weight_decay: bool=False, foreach: bool | None='```(None)```', maximize: bool=False, capturable: bool=False, differentiable: bool=False)` | Implements NAdam algorithm. |
| [ ] | torch | torch.optim | RAdam | torch.optim.RAdam | `(params: ParamsT, lr: float | Tensor=0.001, betas: tuple[float, float]=(0.9, 0.999), eps: float=1e-08, weight_decay: float=0, decoupled_weight_decay: bool=False, foreach: bool | None='```(None)```', maximize: bool=False, capturable: bool=False, differentiable: bool=False)` | Implements RAdam algorithm. |
| [ ] | torch | torch.optim | RMSprop | torch.optim.RMSprop | `(params: ParamsT, lr: float | Tensor=0.01, alpha: float=0.99, eps: float=1e-08, weight_decay: float=0, momentum: float=0, centered: bool=False, capturable: bool=False, foreach: bool | None='```(None)```', maximize: bool=False, differentiable: bool=False)` | Implements RMSprop algorithm. |
| [ ] | torch | torch.optim | Rprop | torch.optim.Rprop | `(params: ParamsT, lr: float | Tensor=0.01, etas: tuple[float, float]=(0.5, 1.2), step_sizes: tuple[float, float]=(1e-06, 50), capturable: bool=False, foreach: bool | None='```(None)```', maximize: bool=False, differentiable: bool=False)` | Implements the resilient backpropagation algorithm. |
| [ ] | torch | torch.optim | SGD | torch.optim.SGD | `(params: ParamsT, lr: float | Tensor=0.001, momentum: float=0, dampening: float=0, weight_decay: float | Tensor=0, nesterov: bool=False, maximize: bool=False, foreach: bool | None='```(None)```', differentiable: bool=False, fused: bool | None='```(None)```')` | Implements stochastic gradient descent (optionally with momentum). |
| [ ] | torch | torch.optim | SparseAdam | torch.optim.SparseAdam | `(params: ParamsT, lr: float | Tensor=0.001, betas: tuple[float, float]=(0.9, 0.999), eps: float=1e-08, maximize: bool=False)` | SparseAdam implements a masked version of the Adam algorithm suitable for sparse gradients. Curre... |
| [ ] | torch | torch.utils.data | BatchSampler | torch.utils.data.BatchSampler | `(sampler: Sampler | Iterable, batch_size: int, drop_last: bool) -> NoneType` | Wraps another sampler to yield a mini-batch of indices. |
| [ ] | torch | torch.utils.data | ChainDataset | torch.utils.data.ChainDataset | `(datasets: iterable of IterableDataset) -> NoneType` | Dataset for chaining multiple :class:`IterableDataset` s. |
| [ ] | torch | torch.utils.data | ConcatDataset | torch.utils.data.ConcatDataset | `(datasets: list[Dataset[_T_co]], cumulative_sizes: list[int]) -> NoneType` | Dataset as a concatenation of multiple datasets. |
| [ ] | torch | torch.utils.data | DFIterDataPipe | torch.utils.data.DFIterDataPipe | `(args, kwargs)` | Iterable-style DataPipe. |
| [ ] | torch | torch.utils.data | DataChunk | torch.utils.data.DataChunk | `(items: Iterable[_T]) -> NoneType` | Built-in mutable sequence. |
| [ ] | torch | torch.utils.data | DataLoader | torch.utils.data.DataLoader | `(dataset: Dataset[_T_co], batch_size: int | None=1, shuffle: bool | None=None, sampler: Sampler | Iterable | None=None, batch_sampler: Sampler[list] | Iterable[list] | None=None, num_workers: int=0, collate_fn: _collate_fn_t | None=None, pin_memory: bool=False, drop_last: bool=False, timeout: float=0, worker_init_fn: _worker_init_fn_t | None=None, multiprocessing_context=None, generator=None, prefetch_factor: int | None=None, persistent_workers: bool=False, pin_memory_device: str='', in_order: bool=True)` | Data loader combines a dataset and a sampler, and provides an iterable over the given dataset. |
| [ ] | torch | torch.utils.data | Dataset | torch.utils.data.Dataset | `(args, kwargs)` | An abstract class representing a :class:`Dataset`. |
| [ ] | torch | torch.utils.data | DistributedSampler | torch.utils.data.DistributedSampler | `(dataset: Dataset, num_replicas: int | None='```(None)```', rank: int | None='```(None)```', shuffle: bool | None=True, seed: int | None=0, drop_last: bool | None=False) -> NoneType` | Sampler that restricts data loading to a subset of the dataset. |
| [ ] | torch | torch.utils.data | IterDataPipe | torch.utils.data.IterDataPipe | `(functions: dict[str, Callable]={}, reduce_ex_hook: Callable | None='```(None)```', getstate_hook: Callable | None='```(None)```', str_hook: Callable | None='```(None)```', repr_hook: Callable | None='```(None)```', _valid_iterator_id: int | None='```(None)```', _number_of_samples_yielded: int=0, _snapshot_state: _SnapshotState='_SnapshotState', _fast_forward_iterator: Iterator | None='```(None)```')` | Iterable-style DataPipe. |
| [ ] | torch | torch.utils.data | IterableDataset | torch.utils.data.IterableDataset | `(args, kwargs)` | An iterable Dataset. |
| [ ] | torch | torch.utils.data | MapDataPipe | torch.utils.data.MapDataPipe | `(functions: dict[str, Callable]={}, reduce_ex_hook: Callable | None='```(None)```', getstate_hook: Callable | None='```(None)```', str_hook: Callable | None='```(None)```', repr_hook: Callable | None='```(None)```')` | Map-style DataPipe. |
| [ ] | torch | torch.utils.data | RandomSampler | torch.utils.data.RandomSampler | `(data_source: Sized, replacement: bool=False, num_samples: int | None='```(None)```', generator: Generator='```(None)```') -> NoneType` | Samples elements randomly. If without replacement, then sample from a shuffled dataset. |
| [ ] | torch | torch.utils.data | Sampler | torch.utils.data.Sampler | `(data_source: Dataset='```(None)```') -> NoneType` | Base class for all Samplers. |
| [ ] | torch | torch.utils.data | SequentialSampler | torch.utils.data.SequentialSampler | `(data_source: Sized) -> NoneType` | Samples elements sequentially, always in the same order. |
| [ ] | torch | torch.utils.data | StackDataset | torch.utils.data.StackDataset | `(args: Dataset=(), kwargs: Dataset, datasets: tuple | dict) -> NoneType` | Dataset as a stacking of multiple datasets. |
| [ ] | torch | torch.utils.data | Subset | torch.utils.data.Subset | `(dataset: Dataset[_T_co], indices: Sequence[int]) -> NoneType` | Subset of a dataset at specified indices. |
| [ ] | torch | torch.utils.data | SubsetRandomSampler | torch.utils.data.SubsetRandomSampler | `(indices: Sequence[int], generator: Generator='```(None)```') -> NoneType` | Samples elements randomly from a given list of indices, without replacement. |
| [ ] | torch | torch.utils.data | TensorDataset | torch.utils.data.TensorDataset | `(tensors: tuple[Tensor, ...]=()) -> NoneType` | Dataset wrapping tensors. |
| [ ] | torch | torch.utils.data | WeightedRandomSampler | torch.utils.data.WeightedRandomSampler | `(weights: torch.Tensor, num_samples: int, replacement: bool=True, generator: Generator='```(None)```') -> NoneType` | Samples elements from ``[0,..,len(weights)-1]`` with given probabilities (weights). |
| [ ] | torch | torch.utils.data | functional_datapipe | torch.utils.data.functional_datapipe | `(name: str, enable_df_api_tracing: bool=False) -> NoneType` | No docstring available. |
| [ ] | torch | torch.utils.data | guaranteed_datapipes_determinism | torch.utils.data.guaranteed_datapipes_determinism | `(prev: bool) -> NoneType` | No docstring available. |
| [ ] | torch | torch.utils.data | non_deterministic | torch.utils.data.non_deterministic | `(cls: type[IterDataPipe] | None='```(None)```', deterministic_fn: Callable[[], bool], arg: type[IterDataPipe] | Callable[[], bool]) -> NoneType` | No docstring available. |
| [ ] | torch | torch.utils.data | runtime_validation_disabled | torch.utils.data.runtime_validation_disabled | `(prev: bool) -> NoneType` | No docstring available. |
