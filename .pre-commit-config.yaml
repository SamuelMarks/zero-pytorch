repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: Ruff Format
        entry: ruff format .
        language: system
        types: [python]
        pass_filenames: false

      - id: ruff-check
        name: Ruff Check (Fix)
        entry: ruff check --fix .
        language: system
        types: [python]
        pass_filenames: false

      - id: pytest-coverage
        name: Run Pytest Coverage
        entry: bash -c "PYTHONPATH=../ml-switcheroo-compiler/src:src pytest --cov=. --cov-report=term-missing"
        language: system
        types: [python]
        pass_filenames: false

      - id: update-badges
        name: Update Coverage Shields
        entry: python3 scripts/update_badges.py
        language: system
        types: [python]
        pass_filenames: false

      - id: check-dependencies
        name: Check Dependencies
        entry: python3 scripts/check_dependencies.py
        language: system
        files: ^src/.*\.py$
        pass_filenames: false


      - id: check-api-compliance
        name: Check API Conformance
        entry: env PYTHONPATH=src:../ml-switcheroo-compiler/src python3 scripts/check_compliance.py
        language: system
        files: ^(SEMANTIC_PLAN\.md|src/.*\.py)$
        pass_filenames: false

      - id: check-api-snapshots
        name: Check API Snapshots
        entry: env PYTHONPATH=src:../ml-switcheroo-compiler/src python3 scripts/check_api_snapshots.py
        language: system
        files: ^(src/.*\.py|ml_snapshots\.txt)$
        pass_filenames: false

      - id: build
        name: Build Package
        entry: python3 -m build
        language: system
        pass_filenames: false
